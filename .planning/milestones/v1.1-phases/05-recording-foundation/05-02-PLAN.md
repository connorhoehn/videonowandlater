---
phase: 05-recording-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/src/handlers/recording-started.ts
  - backend/src/handlers/recording-ended.ts
  - backend/src/repositories/session-repository.ts
  - backend/src/handlers/replenish-pool.ts
  - infra/lib/stacks/session-stack.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-06
  - REC-08

must_haves:
  truths:
    - "Recording-started event sets session recordingStatus to 'processing'"
    - "Recording-ended event populates final metadata and sets status to 'available' or 'failed'"
    - "Broadcast channels created with recordingConfigurationArn attached"
    - "Hangout stages created with recordingConfigurationArn attached"
    - "Repository supports updating recording metadata fields"
  artifacts:
    - path: "backend/src/handlers/recording-started.ts"
      provides: "Lambda handler for Recording Start events"
      min_lines: 30
    - path: "backend/src/handlers/recording-ended.ts"
      provides: "Extended handler with metadata extraction"
      min_lines: 60
    - path: "backend/src/repositories/session-repository.ts"
      provides: "updateRecordingMetadata method"
      exports: ["updateRecordingMetadata"]
    - path: "backend/src/handlers/replenish-pool.ts"
      provides: "Pool creation with recording config attached"
      min_lines: 100
  key_links:
    - from: "recording-started.ts"
      to: "session-repository.ts"
      via: "updateRecordingMetadata call"
      pattern: "updateRecordingMetadata.*recordingStatus.*processing"
    - from: "recording-ended.ts"
      to: "session-repository.ts"
      via: "updateRecordingMetadata call"
      pattern: "updateRecordingMetadata.*recordingDuration"
    - from: "replenish-pool.ts"
      to: "RecordingConfiguration"
      via: "CreateChannelCommand with recordingConfigurationArn"
      pattern: "recordingConfigurationArn.*process\\.env"
    - from: "EventBridge rules"
      to: "recording-started.ts, recording-ended.ts"
      via: "Lambda targets in SessionStack"
      pattern: "addTarget.*recordingStartedFn"
---

<objective>
Implement recording lifecycle handlers and integrate RecordingConfiguration into resource pool management.

Purpose: Complete the recording automation loop - handlers capture EventBridge events to track recording status and extract metadata, pool replenishment attaches RecordingConfiguration so all sessions auto-record from creation.

Output: Working recording-started and recording-ended Lambda handlers, extended session repository with recording metadata updates, pool replenishment creating recording-ready resources, EventBridge rules wired to handlers.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/connorhoehn/Projects/videonowandlater/.planning/PROJECT.md
@/Users/connorhoehn/Projects/videonowandlater/.planning/ROADMAP.md
@/Users/connorhoehn/Projects/videonowandlater/.planning/STATE.md
@/Users/connorhoehn/Projects/videonowandlater/.planning/phases/05-recording-foundation/05-CONTEXT.md
@/Users/connorhoehn/Projects/videonowandlater/.planning/phases/05-recording-foundation/05-RESEARCH.md
@/Users/connorhoehn/Projects/videonowandlater/.planning/phases/05-recording-foundation/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types from Plan 05-01 -->
From backend/src/domain/session.ts:
```typescript
export enum RecordingStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  AVAILABLE = 'available',
  FAILED = 'failed',
}

export interface Session {
  sessionId: string;
  userId: string;
  sessionType: SessionType;
  status: SessionStatus;
  claimedResources: ClaimedResources;
  createdAt: string;
  startedAt?: string;
  endedAt?: string;
  version: number;
  recordingS3Path?: string;
  recordingDuration?: number;
  thumbnailUrl?: string;
  recordingHlsUrl?: string;
  recordingStatus?: RecordingStatus;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Recording-Started Handler and Extend Repository</name>
  <files>backend/src/handlers/recording-started.ts, backend/src/repositories/session-repository.ts</files>
  <action>
**Create recording-started.ts handler:**

1. Import EventBridge event type, Session types, repository functions
2. Define RecordingStartDetail interface matching IVS event structure:
   - channel_arn: string (for broadcasts) OR stage_arn: string (for hangouts)
   - recording_s3_key_prefix: string
   - event_name: 'Recording Start'
3. Handler logic:
   - Extract channel_arn or stage_arn from event.detail
   - Find session by resource ARN (scan claimedResources.channelArn or claimedResources.stageArn)
   - Call updateRecordingMetadata with sessionId, { recordingStatus: 'processing', recordingS3Path: event.detail.recording_s3_key_prefix }
   - Handle errors (log and continue - don't fail event processing)
4. Export as handler function with typed EventBridgeEvent parameter

**Extend session-repository.ts:**

Add updateRecordingMetadata function:
- Parameters: tableName (string), sessionId (string), metadata (Partial<Pick<Session, recording fields>>)
- Build UpdateCommand with dynamic UpdateExpression for provided fields only
- Include version increment for optimistic locking (existing pattern from repository)
- Use ExpressionAttributeNames to avoid reserved words
- Return void (fire-and-forget update)

Follow existing repository pattern (use @aws-sdk/lib-dynamodb UpdateCommand, PK/SK pattern, version increment).
  </action>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <done>recording-started.ts handler exists and processes Recording Start events. session-repository.ts exports updateRecordingMetadata function. Code compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Recording-Ended Handler with Metadata Extraction</name>
  <files>backend/src/handlers/recording-ended.ts</files>
  <action>
Extend existing recording-ended.ts handler to store recording metadata:

1. **Add RecordingEndDetail interface fields:**
   - recording_s3_bucket_name: string
   - recording_s3_key_prefix: string
   - recording_duration_ms: number
   - recording_status: 'Recording End' | 'Recording End Failure'

2. **Compute CloudFront URLs:**
   - Read CLOUDFRONT_DOMAIN from process.env (set by SessionStack)
   - Construct recordingHlsUrl: `https://${cloudFrontDomain}/${recording_s3_key_prefix}/playlist.m3u8`
   - Construct thumbnailUrl: `https://${cloudFrontDomain}/${recording_s3_key_prefix}/thumb-0.jpg`

3. **Update session metadata:**
   - After existing session state transition (ENDING → ENDED)
   - Call updateRecordingMetadata with:
     - recordingDuration: event.detail.recording_duration_ms
     - recordingHlsUrl: computed URL
     - thumbnailUrl: computed URL
     - recordingStatus: event.detail.recording_status === 'Recording End' ? 'available' : 'failed'

4. **Error handling:**
   - Wrap metadata update in try/catch
   - Log errors but don't fail session state transition
   - Success criteria: "Reconnect windows handled" means UI shows processing state, not handler logic

Keep existing functionality (session state transition, pool resource release). Add metadata extraction as additional step. Use existing imports and patterns from handler.
  </action>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <done>recording-ended.ts handler extracts metadata from EventBridge event detail. CloudFront URLs computed from S3 path. Session updated with recordingDuration, recordingHlsUrl, thumbnailUrl, and final recordingStatus. Code compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pool Replenishment and Wire EventBridge Targets</name>
  <files>backend/src/handlers/replenish-pool.ts, infra/lib/stacks/session-stack.ts</files>
  <action>
**Extend replenish-pool.ts:**

1. Read RECORDING_CONFIGURATION_ARN from process.env
2. Modify CreateChannelCommand calls:
   - Add recordingConfigurationArn parameter with env var value
   - Applies to all broadcast channel creation logic
3. Modify CreateStageCommand calls:
   - Add autoParticipantRecordingConfiguration parameter:
     - storageConfigurationArn: RECORDING_CONFIGURATION_ARN
     - mediaTypes: ['AUDIO_VIDEO']
   - Applies to all hangout stage creation logic

Follow existing pool replenishment pattern (MIN_CHANNELS/STAGES loop, error handling, DynamoDB writes).

**Wire EventBridge targets in SessionStack:**

1. Create NodejsFunction for recording-started handler:
   - Entry: backend/src/handlers/recording-started.ts
   - Environment: TABLE_NAME (existing pattern)
   - Timeout: 30 seconds
   - Grant DynamoDB read/write permissions (table.grantReadWriteData)

2. Create NodejsFunction for recording-ended handler (may already exist):
   - If exists, extend environment with CLOUDFRONT_DOMAIN
   - If new, follow recording-started pattern

3. Wire Lambda targets to EventBridge rules (created in Plan 05-01):
   - recordingStartRule.addTarget(new targets.LambdaFunction(recordingStartedFn))
   - recordingEndRule.addTarget(new targets.LambdaFunction(recordingEndedFn))

4. Add RECORDING_CONFIGURATION_ARN to replenish-pool function environment:
   - Use recordingConfig.attrArn from Plan 05-01

5. Add CLOUDFRONT_DOMAIN to recording-ended function environment:
   - Use distribution.distributionDomainName from Plan 05-01
  </action>
  <verify>
    <automated>cd infra && npm run build && cd ../backend && npm run build</automated>
  </verify>
  <done>replenish-pool.ts attaches RecordingConfiguration ARN when creating channels and stages. SessionStack wires recording-started and recording-ended handlers to EventBridge rules. Environment variables set for all handlers. Both infra and backend packages compile successfully.</done>
</task>

</tasks>

<verification>
**Handler Verification:**
1. Grep for updateRecordingMetadata calls in recording-started.ts and recording-ended.ts
2. Verify recording-started sets recordingStatus='processing'
3. Verify recording-ended computes CloudFront URLs and sets final status
4. Check error handling exists (try/catch blocks)

**Integration Verification:**
1. Grep for recordingConfigurationArn in replenish-pool.ts CreateChannelCommand
2. Grep for autoParticipantRecordingConfiguration in CreateStageCommand
3. Verify SessionStack adds Lambda targets to EventBridge rules
4. Check environment variables set (RECORDING_CONFIGURATION_ARN, CLOUDFRONT_DOMAIN, TABLE_NAME)

**End-to-End Flow:**
1. Deploy CDK stack (cdk deploy SessionStack)
2. Trigger pool replenishment (EventBridge scheduled rule or manual invoke)
3. Check DynamoDB pool items have ARNs referencing RecordingConfiguration
4. Verify EventBridge rules exist with Lambda targets attached
</verification>

<success_criteria>
**Measurable Completion:**
- [ ] recording-started.ts handler exists and processes Recording Start events
- [ ] recording-ended.ts handler extracts metadata and computes CloudFront URLs
- [ ] session-repository.ts exports updateRecordingMetadata function
- [ ] replenish-pool.ts attaches recordingConfigurationArn to CreateChannel and CreateStage commands
- [ ] SessionStack wires recording-started and recording-ended handlers to EventBridge rules
- [ ] All handlers have required environment variables set
- [ ] Infra and backend packages compile successfully
- [ ] CDK deployment succeeds with new Lambda functions deployed
- [ ] Manual test: Create session → stream starts → EventBridge Recording Start event → session recordingStatus updates to 'processing'
</success_criteria>

<output>
After completion, create `.planning/phases/05-recording-foundation/05-02-SUMMARY.md` documenting:
- Handler implementations (recording-started and recording-ended logic)
- Repository extension (updateRecordingMetadata signature)
- Pool replenishment changes (recordingConfigurationArn attachment)
- Integration verification results (manual test of recording flow)
- Next phase dependencies (Phase 6 can query sessions with recordingStatus='available' for home feed)
</output>
