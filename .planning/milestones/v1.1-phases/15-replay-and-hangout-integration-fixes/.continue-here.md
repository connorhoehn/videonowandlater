---
phase: 15-replay-and-hangout-integration-fixes
task: ad-hoc
status: in_progress
last_updated: 2026-03-05T19:31:36.625Z
---

<current_state>
Live debugging session for IVS → EventBridge → Lambda recording pipeline.
The recording-ended Lambda is now firing correctly (verified session 8af8b592).
Final outstanding issue: wrong HLS/thumbnail URL paths stored in DynamoDB.
Fix deployed but not yet verified end-to-end with a fresh session.
</current_state>

<completed_work>

- Phase 1 (Observability): Created `backend/src/handlers/ivs-event-audit.ts` — catch-all Lambda for ALL aws.ivs EventBridge events. Logs to CloudWatch log group `VNL-Session-IvsEventAuditLogGroup1F2A3C7D-MMMhXfXDXpMf`
- Phase 2 (Permissions): Added explicit `addPermission()` on all 5 EventBridge-triggered Lambdas (stream-started, stream-ended, recording-started, recording-ended × 2 rules). Added SQS DLQ to recording targets with explicit SendMessage policy. Deployed.
- Bug fix: `stream-started.ts` and `stream-ended.ts` were reading `event.resources?.[0]` — confirmed this IS correct for IVS Stream State Change events (resources[0] = channel ARN). Added full event logging (`Raw event: {...}`) to both handlers.
- Bug fix (ROOT CAUSE of recording pipeline): EventBridge RecordingStartRule and RecordingEndRuleV2 were filtering on `detail.event_name` but IVS Recording State Change events use `detail.recording_status`. Changed both rules to `recording_status: ['Recording Start']` and `recording_status: ['Recording End']`. Deployed and VERIFIED — recording-ended Lambda now fires correctly.
- Bug fix: `recording-ended.ts` was building wrong S3 paths. Changed to correct paths:
  - HLS: `${prefix}/media/hls/master.m3u8` (was `${prefix}/master.m3u8`)
  - Thumbnail: `${prefix}/media/thumbnails/thumb0.jpg` (was `${prefix}/thumb-0.jpg`)
- Deployed URL path fix. NOT YET VERIFIED with a fresh session.

</completed_work>

<remaining_work>

- Verify recording URL path fix: start a new broadcast session, end it, confirm `recordingHlsUrl` in DynamoDB has `/media/hls/master.m3u8` path, confirm replay page loads video without 403
- Fix the race condition warning: `stream-ended.ts` logs "Invalid transition from ending to ending" because frontend's `end-session` API already sets ENDING before IVS fires the Stream End event. The `updateSessionStatus` should gracefully handle already-ENDING state (no-op instead of error)
- Fix the `[useReplayPlayer] skipped — no recordingHlsUrl` double-log: replay page is fetched before recording metadata is written. May need polling or a "processing" state in the UI
- Multiple sessions stuck in ENDING state (pre-fix): `f610d16c`, `98b73e3c`, `a31766a9`, `3a6443fd`, `52897f9b`, `bd4fce40` — need manual DynamoDB cleanup or a backfill script

</remaining_work>

<decisions_made>

- IVS Stream State Change events: channel ARN is in `event.resources[0]` (NOT `event.detail.channel_arn` which doesn't exist)
- IVS Recording State Change events: recording status is in `event.detail.recording_status` (NOT `event.detail.event_name`)
- IVS Recording S3 structure: `{prefix}/media/hls/master.m3u8` and `{prefix}/media/thumbnails/thumb0.jpg`
- Audit Lambda log group: CDK-named `VNL-Session-IvsEventAuditLogGroup1F2A3C7D-MMMhXfXDXpMf` (NOT `/aws/lambda/...`)
- Stack name: `VNL-Session` (not `VnlSessionStack`)

</decisions_made>

<blockers>

- None blocking. All deployed. Needs fresh session test.

</blockers>

<context>
We were systematically debugging why sessions stuck in ENDING never transitioned to ENDED. The entire recording pipeline was broken due to a wrong EventBridge filter field (`event_name` vs `recording_status`). The audit Lambda was key to diagnosing this — it captured the raw IVS event and showed the actual field names. Once that was fixed, recording-ended fired correctly. The last remaining issue is the CloudFront 403 due to wrong URL paths, which is now fixed and deployed but unverified.
</context>

<next_action>
Start a new BROADCAST session, go live for 10+ seconds, end it. Check:
1. `aws dynamodb scan --table-name vnl-sessions --filter-expression "sessionId = :sid" --expression-attribute-values '{":sid":{"S":"NEW_SESSION_ID"}}' --query "Items[0].{status:status.S, recordingHlsUrl:recordingHlsUrl.S, thumbnailUrl:thumbnailUrl.S}" --output json`
2. Navigate to `/replay/NEW_SESSION_ID` — video should play without 403
3. If working, clean up stuck ENDING sessions in DynamoDB
</next_action>
