---
phase: 02-session-model-and-resource-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/domain/session.ts
  - backend/src/domain/resource-pool.ts
  - backend/src/domain/types.ts
  - infra/lib/stacks/session-stack.ts
  - infra/bin/app.ts
autonomous: true
requirements: [SESS-01, SESS-04]

must_haves:
  truths:
    - "Session lifecycle state machine transitions from creating -> live -> ending -> ended"
    - "Session objects contain sessionId (not AWS ARNs) in user-facing data structures"
    - "DynamoDB table with GSI exists for storing sessions and pool items"
  artifacts:
    - path: "backend/src/domain/session.ts"
      provides: "Session entity type, SessionStatus enum, SessionType enum, state transition validation"
      min_lines: 40
      exports: ["Session", "SessionStatus", "SessionType", "canTransition"]
    - path: "backend/src/domain/resource-pool.ts"
      provides: "ResourcePoolItem entity type, Status enum, ResourceType enum"
      min_lines: 30
      exports: ["ResourcePoolItem", "Status", "ResourceType"]
    - path: "backend/src/domain/types.ts"
      provides: "Shared type definitions"
      min_lines: 10
    - path: "infra/lib/stacks/session-stack.ts"
      provides: "DynamoDB table with PK/SK and status GSI"
      min_lines: 50
      exports: ["SessionStack"]
    - path: "infra/bin/app.ts"
      provides: "SessionStack instantiation"
      contains: "new SessionStack"
  key_links:
    - from: "infra/bin/app.ts"
      to: "infra/lib/stacks/session-stack.ts"
      via: "import and instantiate"
      pattern: "import.*SessionStack"
    - from: "infra/lib/stacks/session-stack.ts"
      to: "aws-cdk-lib/aws-dynamodb"
      via: "Table construct with GSI"
      pattern: "new dynamodb\\.Table.*addGlobalSecondaryIndex"
---

<objective>
Establish the foundational session domain model and DynamoDB infrastructure for Phase 2's resource pool pattern.

Purpose: Create type-safe domain entities (Session, ResourcePoolItem) with lifecycle state machines and deploy a single-table DynamoDB design with status GSI for efficient pool queries. This foundation enables subsequent plans to implement pool replenishment and atomic claims.

Output: TypeScript domain types with validation logic, CDK SessionStack with DynamoDB table and GSI, table deployed to AWS.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-model-and-resource-pool/02-RESEARCH.md

# Phase 1 artifacts
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md

<interfaces>
<!-- Existing CDK stack patterns from Phase 1 -->

From infra/bin/app.ts:
```typescript
import { App } from 'aws-cdk-lib';
import { AuthStack } from '../lib/stacks/auth-stack';
import { ApiStack } from '../lib/stacks/api-stack';
const app = new App();
const env = { region: 'us-east-1' };
```

From infra/lib/stacks/api-stack.ts:
```typescript
import { Stack, StackProps, CfnOutput } from 'aws-cdk-lib';
import { Construct } from 'constructs';

export interface ApiStackProps extends StackProps {
  userPool: cognito.UserPool;
  userPoolClient: cognito.UserPoolClient;
}

export class ApiStack extends Stack {
  constructor(scope: Construct, id: string, props: ApiStackProps) {
    super(scope, id, props);
    // Stack implementation
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create session and resource pool domain models</name>
  <files>
    backend/src/domain/session.ts
    backend/src/domain/resource-pool.ts
    backend/src/domain/types.ts
  </files>
  <behavior>
    - Test 1: SessionStatus enum exports creating, live, ending, ended values
    - Test 2: canTransition() returns true for valid transitions (creating->live, live->ending, ending->ended) and false for invalid transitions
    - Test 3: SessionType enum exports BROADCAST and HANGOUT
    - Test 4: Session interface has required fields: sessionId, userId, sessionType, status, claimedResources, version, createdAt
    - Test 5: ResourcePoolItem interface has required fields: resourceType, resourceArn, resourceId, status, version, createdAt
    - Test 6: Status enum exports AVAILABLE, CLAIMED, ENDED
    - Test 7: ResourceType enum exports CHANNEL, STAGE, ROOM
  </behavior>
  <action>
    Create three domain files following the research patterns:

    **backend/src/domain/types.ts** - Shared types:
    - Export Status enum: AVAILABLE = 'AVAILABLE', CLAIMED = 'CLAIMED', ENDED = 'ENDED'
    - Export ResourceType enum: CHANNEL = 'CHANNEL', STAGE = 'STAGE', ROOM = 'ROOM'

    **backend/src/domain/session.ts** - Session entity (Pattern 4 from research):
    - Export SessionStatus enum: CREATING = 'creating', LIVE = 'live', ENDING = 'ending', ENDED = 'ended'
    - Export SessionType enum: BROADCAST = 'BROADCAST', HANGOUT = 'HANGOUT'
    - Export Session interface with fields:
      - sessionId: string
      - userId: string
      - sessionType: SessionType
      - status: SessionStatus
      - claimedResources: { channel?: string; stage?: string; chatRoom: string }
      - createdAt: string (ISO8601)
      - startedAt?: string
      - endedAt?: string
      - version: number (for optimistic locking)
    - Export canTransition(from: SessionStatus, to: SessionStatus): boolean function
      - Valid transitions: creating->live, live->ending, ending->ended
      - Return true if transition is valid, false otherwise

    **backend/src/domain/resource-pool.ts** - Pool item entity (Pattern 1 from research):
    - Import Status and ResourceType from ./types
    - Export ResourcePoolItem interface with fields:
      - resourceType: ResourceType
      - resourceArn: string
      - resourceId: string (last segment of ARN)
      - status: Status
      - version: number
      - createdAt: string
      - claimedAt: string | null
      - claimedBy: string | null (sessionId when CLAIMED)
      - ingestEndpoint?: string (for CHANNEL)
      - playbackUrl?: string (for CHANNEL)
      - streamKey?: string (for CHANNEL - per Pitfall 5)
      - endpoints?: { playback: string; ingest: string } (for STAGE)

    Use TypeScript strict mode. No implementation code yet - pure types and enums.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater && npx tsc --noEmit --project backend/tsconfig.json</automated>
  </verify>
  <done>
    All three domain files exist with exported types. TypeScript compilation succeeds with no errors. canTransition function correctly validates state machine transitions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DynamoDB SessionStack with single-table design and status GSI</name>
  <files>
    infra/lib/stacks/session-stack.ts
    infra/bin/app.ts
  </files>
  <action>
    Create SessionStack following Pattern 1 (single-table design) from research:

    **infra/lib/stacks/session-stack.ts:**
    - Import Stack, StackProps, CfnOutput, RemovalPolicy, Duration from aws-cdk-lib
    - Import * as dynamodb from 'aws-cdk-lib/aws-dynamodb'
    - Import Construct from 'constructs'
    - Export SessionStack class extending Stack
    - Export public readonly table: dynamodb.Table property for cross-stack access
    - In constructor:
      - Create DynamoDB table named 'vnl-sessions'
      - Partition key: PK (STRING)
      - Sort key: SK (STRING)
      - Billing mode: PAY_PER_REQUEST (no provisioned capacity)
      - Removal policy: DESTROY (matches Phase 1 pattern)
      - Point-in-time recovery: false (not needed for v1)
      - Add Global Secondary Index named 'GSI1':
        - Partition key: GSI1PK (STRING)
        - Sort key: GSI1SK (STRING)
        - Projection type: ALL
      - Add CfnOutput for tableName

    **infra/bin/app.ts:**
    - Import SessionStack from '../lib/stacks/session-stack'
    - After existing stacks, add: `new SessionStack(app, 'VNL-Session', { env });`
    - Use same env variable (us-east-1 per Phase 1 decision)

    Follow existing Phase 1 stack patterns (constructor injection, RemovalPolicy.DESTROY, no exportName on outputs).
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk synth VNL-Session</automated>
  </verify>
  <done>
    CDK synth produces CloudFormation template for VNL-Session stack with DynamoDB table, GSI definition, and CfnOutput. Table has PK/SK base keys and GSI1PK/GSI1SK index keys. SessionStack class exports table property for use by Lambda functions in later plans.
  </done>
</task>

</tasks>

<verification>
Run the following to verify Phase 2 Plan 1 completion:

1. TypeScript compilation: `cd backend && npx tsc --noEmit`
2. CDK synth all stacks: `cd infra && npx cdk synth`
3. Verify domain exports: `node -e "const s = require('./backend/src/domain/session'); console.log(s.SessionStatus, s.canTransition('creating', 'live'))"`
4. Check CDK output includes VNL-Session stack with DynamoDB::Table resource
5. Grep for GSI definition: `grep -r "GSI1" infra/lib/stacks/session-stack.ts`
</verification>

<success_criteria>
1. Three domain files exist with full TypeScript types and enums
2. canTransition function correctly validates session state machine transitions
3. SessionStack deployed to AWS with DynamoDB table and status GSI
4. CDK synth produces valid CloudFormation for VNL-Session stack
5. No TypeScript errors in backend or infra packages
6. Session and ResourcePoolItem types match research Pattern 1 and Pattern 4 specifications
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-model-and-resource-pool/02-01-SUMMARY.md` following the standard summary template.
</output>
