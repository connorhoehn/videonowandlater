---
phase: 036-x-ray-distributed-tracing
plan: 04
type: execute
wave: 3
depends_on:
  - 036-02
  - 036-03
files_modified:
  - infra/lib/stacks/session-stack.ts
autonomous: false
requirements:
  - TRACE-01
  - TRACE-04

must_haves:
  truths:
    - "CDK stack defines tracing: lambda.Tracing.ACTIVE on all 5 pipeline NodejsFunctions"
    - "CDK synth output shows Tracing: Active for each of the 5 functions"
    - "After deploy, X-Ray console shows all 5 pipeline Lambda nodes in the service map"
    - "Developer can search X-Ray traces by annotation.sessionId filter expression"
  artifacts:
    - path: "infra/lib/stacks/session-stack.ts"
      provides: "Active tracing on recordingEndedFn, transcodeCompletedFn, transcribeCompletedFn, storeSummaryFn, onMediaConvertCompleteFunction"
      contains: "lambda.Tracing.ACTIVE"
  key_links:
    - from: "infra/lib/stacks/session-stack.ts"
      to: "aws-cdk-lib/aws-lambda"
      via: "lambda.Tracing.ACTIVE property on each NodejsFunction"
      pattern: "Tracing\\.ACTIVE"
---

<objective>
Add `tracing: lambda.Tracing.ACTIVE` to all 5 pipeline Lambda CDK definitions and deploy to AWS. Then verify the X-Ray service map shows all 5 nodes and annotations are searchable.

Purpose: TRACE-01 (active tracing enabled) is only satisfied by the CDK property — handler code alone produces zero traces. TRACE-04 (service map) requires a live deployment and real trace emission.
Output: Updated session-stack.ts deployed with active tracing on all 5 pipeline functions.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/036-x-ray-distributed-tracing/036-02-SUMMARY.md
@.planning/phases/036-x-ray-distributed-tracing/036-03-SUMMARY.md

@infra/lib/stacks/session-stack.ts

<interfaces>
<!-- CDK tracing property — add to each NodejsFunction definition -->
```typescript
import * as lambda from 'aws-cdk-lib/aws-lambda';

// recordingEndedFn (line ~371):
const recordingEndedFn = new nodejs.NodejsFunction(this, 'RecordingEnded', {
  runtime: lambda.Runtime.NODEJS_20_X,
  handler: 'handler',
  entry: path.join(__dirname, '../../../backend/src/handlers/recording-ended.ts'),
  timeout: Duration.seconds(30),
  tracing: lambda.Tracing.ACTIVE,  // ADD THIS
  environment: { ... },
  ...
});

// Same property for: transcodeCompletedFn, transcribeCompletedFn, storeSummaryFn, onMediaConvertCompleteFunction
```

<!-- CDK automatically adds xray:PutTraceSegments + xray:PutTelemetryRecords to each function's execution role -->
<!-- No manual IAM changes required                                                                           -->

<!-- Verify command after CDK synth: -->
<!-- cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk synth 2>&1 | grep -A2 "Tracing" -->
<!-- Expected output: lines showing "Tracing: Active" for each of the 5 functions                     -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tracing: lambda.Tracing.ACTIVE to all 5 pipeline NodejsFunctions in session-stack.ts</name>
  <files>infra/lib/stacks/session-stack.ts</files>
  <action>
    Read the current session-stack.ts, focusing on the 5 NodejsFunction definitions:
    - `recordingEndedFn` (~line 371)
    - `transcodeCompletedFn` (~line 613)
    - `transcribeCompletedFn` (~line 660)
    - `storeSummaryFn` (~line 694)
    - `onMediaConvertCompleteFunction` (~line 789)

    For each of these 5 NodejsFunction definitions, add `tracing: lambda.Tracing.ACTIVE` as a property.
    The `lambda` import from `aws-cdk-lib/aws-lambda` is already in scope (used for `lambda.Runtime.NODEJS_20_X`).

    Do NOT add tracing to non-pipeline functions (replenishPoolFn, scanStuckSessionsFn, streamStartedFn, streamEndedFn, recordingStartedFn, startMediaConvertFunction, startTranscribeFn, etc.) — only the 5 pipeline handlers that process recording/transcription/summary events.

    After editing, run CDK synth to verify:
    `cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk synth 2>&1 | grep -i "tracing"` — should show "Active" entries for each of the 5 functions.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk synth 2>&1 | grep -i "tracing" | head -20</automated>
  </verify>
  <done>CDK synth output shows "Tracing: Active" (or equivalent CloudFormation property TracingConfig: Mode: Active) for all 5 pipeline functions. No synth errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy and verify X-Ray service map + annotation search</name>
  <action>
    Deploy the CDK stack: `cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk deploy --require-approval never`
    Then follow the verification steps below to confirm X-Ray traces appear correctly.
  </action>
  <verify>
    <automated>MISSING — X-Ray service map and trace visibility require a live AWS deployment and real pipeline execution; see how-to-verify steps below</automated>
  </verify>
  <done>All 5 pipeline Lambda nodes visible in X-Ray service map. Traces contain SDK subsegments. annotation.sessionId and annotation.pipelineStage filter expressions return results.</done>
  <what-built>
    All 5 pipeline Lambda handlers (recording-ended, transcode-completed, transcribe-completed, store-summary, on-mediaconvert-complete) have been refactored with X-Ray active tracing. CDK stack updated with `tracing: lambda.Tracing.ACTIVE` on all 5 functions. Ready to deploy and verify in AWS X-Ray console.
  </what-built>
  <how-to-verify>
    1. Deploy: `cd /Users/connorhoehn/Projects/videonowandlater/infra && npx cdk deploy --require-approval never`
    2. Trigger a recording: Start a broadcast session from the app, let it run for ~30 seconds, then stop it (this triggers the recording-ended → transcode-completed → transcribe-completed → store-summary pipeline)
    3. Wait ~2-3 minutes for the pipeline to process
    4. Open AWS X-Ray console → Service Map
    5. Verify all 5 Lambda function nodes appear: recording-ended, transcode-completed (TranscodeCompleted), transcribe-completed (TranscribeCompleted), store-summary (StoreSummary), on-mediaconvert-complete (OnMediaConvertComplete)
    6. Click any Lambda node → View traces → open a trace → verify subsegments exist for SDK calls (DynamoDB, S3, Transcribe, Bedrock, etc.)
    7. In X-Ray "Find traces" search, enter: `annotation.sessionId = "your-session-id"` — verify traces are returned
    8. In X-Ray "Find traces" search, enter: `annotation.pipelineStage = "recording-ended"` — verify traces are returned

    Expected: All 5 nodes visible. Subsegments per SDK call. sessionId and pipelineStage annotations searchable.
    Note: Nodes will NOT be connected in a single chain — each stage appears as a separate node. This is expected due to EventBridge→SQS trace context limitation (platform constraint, not a bug).
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 nodes appear with subsegments and annotations are searchable. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
CDK synth shows Tracing: Active for all 5 functions.
Post-deploy X-Ray console shows all 5 pipeline Lambda nodes.
Annotations searchable by sessionId and pipelineStage.
</verification>

<success_criteria>
CDK stack deployed with `tracing: lambda.Tracing.ACTIVE` on all 5 pipeline functions (TRACE-01). X-Ray service map shows all 5 pipeline Lambda nodes after a recording triggers the full pipeline (TRACE-04). Developer can search traces by annotation.sessionId filter expression.
</success_criteria>

<output>
After completion, create `.planning/phases/036-x-ray-distributed-tracing/036-04-SUMMARY.md`
</output>
