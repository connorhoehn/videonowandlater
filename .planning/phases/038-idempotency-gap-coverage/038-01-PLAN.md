---
phase: 038-idempotency-gap-coverage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/handlers/__tests__/transcribe-completed.test.ts
  - backend/src/handlers/__tests__/store-summary.test.ts
autonomous: true
requirements: [IDEM-01, IDEM-02, IDEM-03]

must_haves:
  truths:
    - Re-driving a transcribe-completed message with an already-available transcript produces no second S3 write and no error
    - Re-driving a store-summary message with an already-available AI summary produces no second Bedrock invocation and no error
    - Sending the same message twice concurrently to either handler results in exactly one execution completing and the duplicate being acknowledged
  artifacts:
    - path: backend/src/handlers/__tests__/transcribe-completed.test.ts
      provides: IDEM-01 test case validating idempotent skip
      min_lines: 50
    - path: backend/src/handlers/__tests__/store-summary.test.ts
      provides: IDEM-02 test case validating idempotent skip
      min_lines: 50
  key_links:
    - from: transcribe-completed handler
      to: session state check
      via: getSessionById(tableName, sessionId) before updateTranscriptStatus
      pattern: "transcriptStatus === 'available'"
    - from: store-summary handler
      to: session state check
      via: getSessionById(tableName, sessionId) before Bedrock invocation
      pattern: "aiSummaryStatus === 'available'"
---

<objective>
Define test contracts for idempotency guards in transcribe-completed and store-summary handlers, establishing what "already executed" means for each handler.

Purpose: Test-driven approach ensures idempotency requirements are explicit before implementation. Each test describes the exact scenario (re-drive with existing data) and expected behavior (no-op success).

Output: Test cases for IDEM-01 (transcript exists → skip), IDEM-02 (summary exists → skip), and IDEM-03 (concurrent invocations → exactly one execution)
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/038-idempotency-gap-coverage/038-RESEARCH.md

# Existing test patterns (reference)
@backend/src/handlers/__tests__/transcode-completed.test.ts
@backend/src/handlers/__tests__/transcribe-completed.test.ts
@backend/src/handlers/__tests__/store-summary.test.ts

# Handler implementations (read-only reference)
@backend/src/handlers/transcribe-completed.ts
@backend/src/handlers/store-summary.ts
@backend/src/handlers/transcode-completed.ts

# Repository functions (read-only reference)
@backend/src/repositories/session-repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write IDEM-01 test case (transcribe-completed idempotency)</name>
  <files>backend/src/handlers/__tests__/transcribe-completed.test.ts</files>
  <behavior>
    Test Case 1 (IDEM-01): Handler invoked twice with same sessionId
    - First invocation: session.transcriptStatus = 'processing' (initial state)
    - Second invocation: session.transcriptStatus = 'available' (already completed)
    - Expected behavior:
      - First call: updates session to 'available', emits event, returns success
      - Second call: reads session, sees 'available', logs "already available (idempotent retry)", returns success WITHOUT S3 write or event emission
      - Assertion: updateTranscriptStatus called exactly once
      - Assertion: batchItemFailures empty in both cases
      - Assertion: logger.info contains "Transcript already available (idempotent retry)"
  </behavior>
  <action>
    Add test case at the end of the describe block in transcribe-completed.test.ts (after existing tests).

    Name: "IDEM-01: Second invocation with same sessionId skips S3 write and DynamoDB update (already available)"

    Structure:
    1. Mock getSessionById to return session with transcriptStatus='available' and transcript content
    2. Create SQS event with same sessionId as first execution
    3. Call handler with mocked event
    4. Verify:
       - Result.batchItemFailures is empty (message acknowledged)
       - updateTranscriptStatus was NOT called (no duplicate write)
       - ebClient.send was NOT called (no duplicate event emission)
       - logger.info called with "Transcript already available (idempotent retry)"
    5. Verify S3 GetObjectCommand was called (to fetch transcript for signature verification), but PutObjectCommand was NOT

    Reference transcode-completed.test.ts for ConflictException pattern, but this test focuses on session state check (not exception catching).
  </action>
  <verify>
    <automated>npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-01" 2>&1 | grep -E "(PASS|FAIL|IDEM-01)"</automated>
  </verify>
  <done>Test case written and file saved. Test fails (RED) because idempotency guard not yet implemented in handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write IDEM-02 test case (store-summary idempotency)</name>
  <files>backend/src/handlers/__tests__/store-summary.test.ts</files>
  <behavior>
    Test Case 2 (IDEM-02): Handler invoked twice with same sessionId
    - First invocation: session.aiSummaryStatus = 'processing' (initial state)
    - Second invocation: session.aiSummaryStatus = 'available' (already completed)
    - Expected behavior:
      - First call: fetches transcript, invokes Bedrock, stores summary, returns success
      - Second call: reads session, sees aiSummaryStatus='available' with aiSummary present, logs "already available (idempotent retry)", returns success WITHOUT Bedrock invocation or DynamoDB update
      - Assertion: bedrockClient.send called exactly once (first invocation only)
      - Assertion: updateSessionAiSummary called exactly once (first invocation only)
      - Assertion: batchItemFailures empty in both cases
      - Assertion: logger.info contains "AI summary already available (idempotent retry)"
  </behavior>
  <action>
    Add test case at the end of the describe block in store-summary.test.ts (after existing tests).

    Name: "IDEM-02: Second invocation with same sessionId skips Bedrock invocation (already available)"

    Structure:
    1. Mock getSessionById to:
       - First call returns session with aiSummaryStatus='processing' (first invocation)
       - Second call returns session with aiSummaryStatus='available' and aiSummary='existing summary text' (second invocation)
    2. Mock Bedrock client to return a summary response
    3. Create SQS event with same sessionId
    4. Call handler with mocked event
    5. Verify:
       - Result.batchItemFailures is empty (message acknowledged)
       - bedrockClient.send was NOT called (no duplicate Bedrock invocation)
       - updateSessionAiSummary was NOT called (no duplicate write)
       - logger.info called with "AI summary already available (idempotent retry)"
       - s3Client.send was called once (to fetch transcript, non-blocking for verification)

    Reference store-summary.test.ts existing tests for Bedrock mock setup.
  </action>
  <verify>
    <automated>npm test -- backend/src/handlers/__tests__/store-summary.test.ts -t "IDEM-02" 2>&1 | grep -E "(PASS|FAIL|IDEM-02)"</automated>
  </verify>
  <done>Test case written and file saved. Test fails (RED) because idempotency guard not yet implemented in handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write IDEM-03 test case (concurrent delivery protection)</name>
  <files>backend/src/handlers/__tests__/transcribe-completed.test.ts</files>
  <behavior>
    Test Case 3 (IDEM-03): Handler invoked concurrently with same sessionId (race condition scenario)
    - Simulate SQS at-least-once delivery: same message sent to Lambda twice within 100ms
    - Mock getSessionById to return:
      - First two calls (race): transcriptStatus='processing' (both invocations see initial state)
      - Third call (after first completes): transcriptStatus='available' (second invocation retries after first finishes)
    - Expected behavior:
      - Both invocations return success (batchItemFailures empty)
      - updateTranscriptStatus called exactly once (first invocation only)
      - Second invocation sees 'available' and skips update
      - No S3 PutObject corruption (only one write)
    - Implementation: Use Promise.all with 50ms stagger to simulate concurrent Lambda instances
  </behavior>
  <action>
    Add test case at the end of transcribe-completed.test.ts describe block.

    Name: "IDEM-03: Concurrent invocations (Promise.all race) result in exactly one S3 write"

    Structure:
    1. Mock getSessionById to return:
       - .mockResolvedValueOnce({ transcriptStatus: 'processing' }) // First check, first invocation
       - .mockResolvedValueOnce({ transcriptStatus: 'processing' }) // First check, second invocation (race)
       - .mockResolvedValueOnce({ transcriptStatus: 'available', transcript: '...' }) // Second check, second invocation (after first completes)
    2. Mock S3 GetObject to return transcript JSON
    3. Mock updateTranscriptStatus to succeed
    4. Create SQS event with same sessionId
    5. Invoke handler twice concurrently:
       ```javascript
       const [result1, result2] = await Promise.all([
         handler(event),
         new Promise(resolve => setTimeout(() => resolve(handler(event)), 50))
       ]);
       ```
    6. Verify:
       - Both results have empty batchItemFailures
       - updateTranscriptStatus called exactly once
       - ebClient.send (event emission) called exactly once
       - logger.info shows both invocations completed
  </action>
  <verify>
    <automated>npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-03" 2>&1 | grep -E "(PASS|FAIL|IDEM-03)"</automated>
  </verify>
  <done>Test case written and file saved. Test fails (RED) because idempotency guard not yet implemented in handler.</done>
</task>

</tasks>

<verification>
After Task 3 completes, verify all three test cases exist:

```bash
npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-0[13]"
npm test -- backend/src/handlers/__tests__/store-summary.test.ts -t "IDEM-02"
```

Both suites should show:
- IDEM-01: FAIL (RED) — handler doesn't check session state yet
- IDEM-02: FAIL (RED) — handler doesn't check session state yet
- IDEM-03: FAIL (RED) — concurrent scenario not yet guarded

This is correct — RED phase of TDD. Wave 2 will implement the guards to make tests GREEN.
</verification>

<success_criteria>
- Three test cases written in correct files
- All tests fail (RED) due to missing idempotency guard logic
- Test assertions match requirement definitions:
  - IDEM-01: Session state check prevents duplicate S3 write
  - IDEM-02: Session state check prevents duplicate Bedrock invocation
  - IDEM-03: Concurrent race condition results in exactly one side effect
- Tests are runnable with `npm test -- --testNamePattern="IDEM-0[123]"`
</success_criteria>

<output>
After completion, create `.planning/phases/038-idempotency-gap-coverage/038-01-SUMMARY.md` with:
- Test files created
- Test case names and locations (line numbers)
- Current status (RED — idempotency guards not yet implemented)
- Next wave action (implement idempotency checks in processEvent functions)
</output>
