---
phase: 038-idempotency-gap-coverage
plan: 03
type: execute
wave: 2
depends_on: [038-01, 038-02]
files_modified:
  - backend/src/handlers/__tests__/transcribe-completed.test.ts
autonomous: true
requirements: [IDEM-03]

must_haves:
  truths:
    - Sending the same message twice concurrently (simulating SQS at-least-once delivery) to transcribe-completed results in exactly one execution completing and the duplicate being acknowledged without side effects
  artifacts:
    - path: backend/src/handlers/__tests__/transcribe-completed.test.ts
      provides: IDEM-03 concurrent delivery test case
      min_lines: 30
  key_links:
    - from: concurrent Lambda invocations
      to: idempotency guard
      via: both invocations race on session state check
      pattern: "session?.transcriptStatus === 'available'"
---

<objective>
Verify that concurrent SQS deliveries (same message sent twice within 100ms) to the transcribe-completed handler result in exactly one successful execution. This tests the race condition scenario where Lambda auto-scales and processes the same message twice before either completes.

Purpose: Ensure idempotency guard handles concurrent delivery without side effect duplication

Output: IDEM-03 test passing, verifying concurrent delivery protection
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/038-idempotency-gap-coverage/038-RESEARCH.md
@.planning/phases/038-idempotency-gap-coverage/038-01-SUMMARY.md (test structure)
@.planning/phases/038-idempotency-gap-coverage/038-02-SUMMARY.md (implementation)

# Handler reference
@backend/src/handlers/transcribe-completed.ts

# Test reference
@backend/src/handlers/__tests__/transcribe-completed.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify IDEM-03 concurrent delivery test passes</name>
  <files>backend/src/handlers/__tests__/transcribe-completed.test.ts</files>
  <action>
    The IDEM-03 test case was written in Plan 01 but not yet passing. Now that idempotency guards exist (Plan 02), run the test to verify it passes.

    Test mechanism:
    - Mock getSessionById to return 'processing' for first two calls (both concurrent invocations see initial state)
    - Mock getSessionById to return 'available' on third call (second invocation's retry after first completes)
    - Create SQS event with same sessionId
    - Invoke handler twice concurrently using Promise.all with 50ms stagger
    - Verify both return success (empty batchItemFailures)
    - Verify updateTranscriptStatus called exactly once
    - Verify ebClient.send (Transcript Stored event) called exactly once

    Race scenario:
    1. First invocation starts: reads session, sees 'processing', proceeds to S3 fetch
    2. Second invocation starts (50ms later): reads session, also sees 'processing', proceeds to S3 fetch
    3. First completes: updates session to 'available', returns success
    4. Second completes: reads updated session, sees 'available', idempotency guard activates, returns success without update

    Expected call counts after both resolve:
    - getSessionById: 3 times (first: initial, second: initial, second-retry: sees completed)
    - updateTranscriptStatus: 1 time (first invocation only)
    - ebClient.send: 1 time (first invocation only)
    - S3 GetObject: 2 times (both fetch transcript, but only first writes)

    Run test:
    ```bash
    npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-03"
    ```

    If test fails:
    - Check mock setup matches concurrent scenario
    - Verify Promise.all stagger timing (50ms allows first to complete before second checks state)
    - Ensure both handler invocations share the same event object (same sessionId)
  </action>
  <verify>
    <automated>npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-03" 2>&1 | grep -E "(PASS|✓|FAIL|✗|updateTranscriptStatus.*once)"</automated>
  </verify>
  <done>IDEM-03 test passing. Concurrent delivery verified: exactly one session update, one event emission despite two simultaneous invocations. Both return success without errors.</done>
</task>

</tasks>

<verification>
After task completes:

```bash
npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts -t "IDEM-03"
```

Expected output:
```
transcribe-completed.test.ts
  IDEM-03: Concurrent invocations... ✓
```

Test passes, confirming:
- Both concurrent invocations return empty batchItemFailures (both acknowledged)
- updateTranscriptStatus called exactly once (no duplicate writes)
- ebClient.send called exactly once (no duplicate events)
- Race condition handled correctly without exception or error

Also verify existing tests still pass:
```bash
npm test -- backend/src/handlers/__tests__/transcribe-completed.test.ts
```

All tests including IDEM-01, IDEM-03, and existing tests should pass.
</verification>

<success_criteria>
- IDEM-03 test passing: Concurrent Promise.all invocations result in exactly one side effect
- Both concurrent invocations return success (batchItemFailures empty)
- No duplicate session updates or event emissions
- Session state check correctly prevents second invocation from re-executing
- All 360 backend tests still passing: `npm test`
- IDEM-01, IDEM-02, IDEM-03 all passing
</success_criteria>

<output>
After completion, create `.planning/phases/038-idempotency-gap-coverage/038-03-SUMMARY.md` with:
- Test results (IDEM-03 passing)
- Concurrent scenario description
- Call count verification (updateTranscriptStatus: 1, ebClient.send: 1)
- All 360 tests passing
- Phase 38 complete: all three idempotency requirements verified (IDEM-01, IDEM-02, IDEM-03)
</output>
