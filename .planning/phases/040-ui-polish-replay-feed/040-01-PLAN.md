---
phase: 040-ui-polish-replay-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/features/replay/TranscriptDisplay.tsx
  - web/src/features/replay/TranscriptDisplay.test.tsx
  - web/src/features/replay/SummaryDisplay.tsx
  - web/src/features/replay/SummaryDisplay.test.tsx
  - web/src/features/replay/ReplayViewer.tsx
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "User clicks a transcript segment and the video player seeks to that timestamp"
    - "Clicking works in both plain segment mode and speaker bubble mode"
    - "AI summary panel shows a spinner when processing, styled card when available, error styling when failed"
    - "SummaryDisplay text content unchanged (backward compatible with existing tests)"
  artifacts:
    - path: "web/src/features/replay/TranscriptDisplay.tsx"
      provides: "Click-to-seek callback on transcript segments"
      contains: "onSeek"
    - path: "web/src/features/replay/TranscriptDisplay.test.tsx"
      provides: "Tests for click-to-seek behavior"
      contains: "onSeek"
    - path: "web/src/features/replay/SummaryDisplay.tsx"
      provides: "Visually distinct states for pending/available/failed"
      contains: "animate-spin"
    - path: "web/src/features/replay/SummaryDisplay.test.tsx"
      provides: "Tests verifying distinct visual elements per state"
      contains: "animate-spin"
    - path: "web/src/features/replay/ReplayViewer.tsx"
      provides: "Seek handler wired from TranscriptDisplay to IVS player"
      contains: "handleSeek"
  key_links:
    - from: "web/src/features/replay/TranscriptDisplay.tsx"
      to: "web/src/features/replay/ReplayViewer.tsx"
      via: "onSeek callback prop"
      pattern: "onSeek.*handleSeek"
    - from: "web/src/features/replay/ReplayViewer.tsx"
      to: "IVS Player SDK"
      via: "playerRef.current.seekTo(seconds)"
      pattern: "seekTo"
---

<objective>
Add click-to-seek functionality to TranscriptDisplay and restyle SummaryDisplay with visually distinct states for processing/available/failed.

Purpose: Users can navigate replay videos by clicking transcript segments (UI-01), and AI summary panels clearly communicate their current state through distinct visual treatments (UI-02).
Output: Updated TranscriptDisplay with onSeek callback, updated SummaryDisplay with spinner/card/error styling, wired seek handler in ReplayViewer.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/040-ui-polish-replay-feed/040-RESEARCH.md

@web/src/features/replay/TranscriptDisplay.tsx
@web/src/features/replay/SummaryDisplay.tsx
@web/src/features/replay/SummaryDisplay.test.tsx
@web/src/features/replay/ReplayViewer.tsx
@web/src/features/replay/useReplayPlayer.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From web/src/features/replay/TranscriptDisplay.tsx:
```typescript
interface TranscriptDisplayProps {
  sessionId: string;
  currentTime: number; // in milliseconds
  authToken: string;
  diarizedTranscriptS3Path?: string;
}

interface TranscriptSegment {
  startTime: number;
  endTime: number;
  text: string;
}

interface SpeakerSegment {
  speaker: string;
  startTime: number;
  endTime: number;
  text: string;
}
```

From web/src/features/replay/SummaryDisplay.tsx:
```typescript
export interface SummaryDisplayProps {
  summary?: string;
  status?: 'pending' | 'available' | 'failed';
  truncate?: boolean;
  className?: string;
}
```

From web/src/features/replay/useReplayPlayer.ts:
```typescript
// Returns { videoRef, syncTime, isPlaying, player }
// player is playerRef.current (IVS Player SDK instance)
// player.seekTo(seconds) — note: NOT milliseconds
// player may be null before initialization
```

From web/src/features/replay/ReplayViewer.tsx:
```typescript
// Line 124: const { videoRef, syncTime } = useReplayPlayer(session?.recordingHlsUrl);
// TranscriptDisplay is rendered at line 415:
// <TranscriptDisplay sessionId={sessionId!} currentTime={syncTime} authToken={authToken} diarizedTranscriptS3Path={session.diarizedTranscriptS3Path} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onSeek callback to TranscriptDisplay + wire in ReplayViewer</name>
  <files>web/src/features/replay/TranscriptDisplay.tsx, web/src/features/replay/TranscriptDisplay.test.tsx, web/src/features/replay/ReplayViewer.tsx</files>
  <behavior>
    - Test 1: When onSeek is provided, clicking a plain segment div calls onSeek with that segment's startTime
    - Test 2: When onSeek is provided, clicking a speaker bubble div calls onSeek with that speaker segment's startTime
    - Test 3: When onSeek is not provided, clicking a segment does not throw (optional callback)
    - Test 4: Segments have cursor-pointer class when onSeek is provided
  </behavior>
  <action>
    1. Create `web/src/features/replay/TranscriptDisplay.test.tsx` with test cases above. Mock fetch to return transcript data. Use @testing-library/react fireEvent.click on segment divs.

    2. Add `onSeek?: (timeMs: number) => void` to `TranscriptDisplayProps` interface in TranscriptDisplay.tsx.

    3. In the plain segment render path (line ~300), add `onClick={() => onSeek?.(segment.startTime)}` and `cursor-pointer` to the segment div className. Add `hover:bg-blue-50` for non-active segments as a click affordance.

    4. In the speaker bubble render path (line ~258), add `onClick={() => onSeek?.(seg.startTime)}` and `cursor-pointer` to the outer flex div.

    5. In ReplayViewer.tsx:
       - Destructure `player` from the useReplayPlayer hook (currently only videoRef and syncTime are destructured at line 124). Change to: `const { videoRef, syncTime, player } = useReplayPlayer(session?.recordingHlsUrl);`
       - Create a `handleSeek` function: `const handleSeek = (timeMs: number) => { if (playerRef is available via player) player.seekTo(timeMs / 1000); }` — guard against null player.
       - IMPORTANT: useReplayPlayer returns `player: playerRef.current` which captures the value at render time. The player is set asynchronously, so `player` from the hook may be stale/null. Instead, access `videoRef.current` and use its `currentTime` setter as a fallback: `if (videoRef.current) videoRef.current.currentTime = timeMs / 1000;` — the IVS player attaches to the video element, so setting currentTime on the underlying HTMLVideoElement works.
       - Pass `onSeek={handleSeek}` to the TranscriptDisplay component at line 415.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run src/features/replay/TranscriptDisplay.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Clicking any transcript segment (plain or speaker bubble) calls onSeek with the segment's startTime in milliseconds. ReplayViewer passes a handleSeek that seeks the video element. All new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle SummaryDisplay with distinct visual states</name>
  <files>web/src/features/replay/SummaryDisplay.tsx, web/src/features/replay/SummaryDisplay.test.tsx</files>
  <behavior>
    - Test 1: Pending state renders an element with animate-spin class (spinner) and text "Generating summary..."
    - Test 2: Available state renders summary text inside a div with bg-blue-50 class (styled card)
    - Test 3: Failed state renders text "Summary unavailable" inside a div with bg-red-50 class (error styling)
    - Test 4: Existing tests still pass (text content "Summary coming soon..." removed — update test to match new text "Generating summary...")
    - Test 5: truncate prop still adds line-clamp-2 to the text element in available state
  </behavior>
  <action>
    1. Update existing tests in SummaryDisplay.test.tsx:
       - Change "Summary coming soon..." assertion to "Generating summary..." (new pending text)
       - Add assertion that pending state has an element with `animate-spin` class
       - Add assertion that available state wraps in a div with `bg-blue-50` class
       - Add assertion that failed state wraps in a div with `bg-red-50` class
       - Keep the "Summary unavailable" text assertion (unchanged)
       - Update the backward compatibility test (undefined status) to expect "Generating summary..."

    2. Restyle SummaryDisplay.tsx:
       - **Pending state**: Replace plain `<p>` with a flex container containing a spinning indicator (`<div className="animate-spin rounded-full h-4 w-4 border-2 border-blue-200 border-t-blue-600" />`) and `<span>` with text "Generating summary...". Apply className to the outer container.
       - **Available state**: Wrap summary text in a styled card div: `<div className="bg-blue-50 border border-blue-100 rounded-lg p-3 {className}">`. Keep `<p>` inside with `line-clamp-2` when truncate is true.
       - **Failed state**: Wrap in `<div className="flex items-center gap-2 bg-red-50 border border-red-100 rounded-lg p-3 {className}">`. Add a red exclamation SVG icon (4x4, text-red-400). Keep "Summary unavailable" text as a `<span>`.

    IMPORTANT: The text "Summary unavailable" MUST remain unchanged for backward compatibility with card components. The text "Summary coming soon..." changes to "Generating summary..." which is acceptable since this is a UI polish phase.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run src/features/replay/SummaryDisplay.test.tsx --reporter=verbose</automated>
  </verify>
  <done>SummaryDisplay renders three visually distinct states: spinner+text for pending, blue card for available, red error card for failed. All existing test assertions updated and passing. truncate and className props still work correctly.</done>
</task>

</tasks>

<verification>
cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run --reporter=verbose
</verification>

<success_criteria>
1. TranscriptDisplay accepts onSeek callback and fires it with startTime on click (both render paths)
2. ReplayViewer wires handleSeek from TranscriptDisplay to the video element
3. SummaryDisplay shows spinner for pending, styled card for available, error card for failed
4. All frontend tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/040-ui-polish-replay-feed/040-01-SUMMARY.md`
</output>
