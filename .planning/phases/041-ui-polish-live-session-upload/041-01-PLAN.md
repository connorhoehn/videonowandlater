---
phase: 041-ui-polish-live-session-upload
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/components/__tests__/ConfirmDialog.test.tsx
  - web/src/features/hangout/__tests__/HangoutPage.test.tsx
  - web/src/features/upload/__tests__/VideoPage.test.tsx
  - web/src/features/upload/__tests__/CommentThread.test.tsx
autonomous: true
requirements:
  - UI-06
  - UI-07
  - UI-08
  - UI-09

must_haves:
  truths:
    - "All four test files exist with failing tests that define the contracts for plan 02 and plan 03"
    - "ConfirmDialog tests define: renders when open, calls onConfirm when confirmed, calls onCancel when cancelled, renders nothing when closed"
    - "HangoutPage tests define: both Leave buttons show ConfirmDialog instead of navigating, ReactionPicker renders when joined, clicking emoji calls sendReaction"
    - "VideoPage tests define: polling starts when session is non-terminal, polling stops when session becomes terminal"
    - "CommentThread tests define: clicking a comment row calls onSeek with videoPositionMs, submission requires syncTime > 0"
  artifacts:
    - path: "web/src/components/__tests__/ConfirmDialog.test.tsx"
      provides: "ConfirmDialog component contract"
    - path: "web/src/features/hangout/__tests__/HangoutPage.test.tsx"
      provides: "UI-06 leave guard + UI-07 reaction parity contracts"
    - path: "web/src/features/upload/__tests__/VideoPage.test.tsx"
      provides: "UI-08 polling contract"
    - path: "web/src/features/upload/__tests__/CommentThread.test.tsx"
      provides: "UI-09 comment seek contract"
  key_links:
    - from: "web/src/components/__tests__/ConfirmDialog.test.tsx"
      to: "web/src/components/ConfirmDialog.tsx"
      via: "import that will fail until plan 02 creates the file"
      pattern: "import.*ConfirmDialog"
    - from: "web/src/features/upload/__tests__/CommentThread.test.tsx"
      to: "web/src/features/upload/CommentThread.tsx"
      via: "onSeek prop that doesn't exist yet"
      pattern: "onSeek"
---

<objective>
Create four failing test files that lock in the behavioral contracts for all Phase 41 requirements. These tests must be RED now, then turned GREEN by plans 02 and 03.

Purpose: Test-first design ensures plans 02 and 03 have unambiguous implementation targets. Executors implement exactly what the tests assert — no guessing.
Output: Four test files with failing tests, one commit per file.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/041-ui-polish-live-session-upload/041-RESEARCH.md

Existing test pattern reference:
@web/src/features/broadcast/__tests__/BroadcastPage.test.tsx
@web/src/features/activity/__tests__/PipelineStatusBadge.test.tsx
</context>

<feature>
  <name>Wave 0 Test Scaffolds — four failing test files</name>
  <files>
    web/src/components/__tests__/ConfirmDialog.test.tsx,
    web/src/features/hangout/__tests__/HangoutPage.test.tsx,
    web/src/features/upload/__tests__/VideoPage.test.tsx,
    web/src/features/upload/__tests__/CommentThread.test.tsx
  </files>

  <behavior>
    ConfirmDialog.test.tsx:
    - renders nothing when isOpen=false
    - renders title + message when isOpen=true
    - calls onConfirm when confirm button is clicked
    - calls onCancel when cancel button is clicked
    - confirm button shows custom confirmLabel when provided
    - confirm button defaults to "Confirm" when confirmLabel not provided

    HangoutPage.test.tsx (vi.mock all heavy deps: useHangout, useChatRoom, useActiveSpeaker, VideoGrid, ChatPanel, react-router-dom):
    - clicking the header "Leave" button (aria-label or text "← Leave") shows ConfirmDialog, does NOT call navigate immediately
    - clicking the controls "Leave" button (text "Leave" in controls bar) shows ConfirmDialog, does NOT call navigate immediately
    - clicking ConfirmDialog "Confirm" calls navigate("/")
    - clicking ConfirmDialog "Cancel" dismisses dialog without navigating
    - ReactionPicker renders in controls bar when isJoined=true
    - clicking an emoji in ReactionPicker calls sendReaction mock

    VideoPage.test.tsx (vi.mock: useHlsPlayer, fetchAuthSession, fetch, react-router-dom):
    - starts polling (setInterval called) when session aiSummaryStatus is "pending"
    - starts polling when session transcriptStatus is "processing"
    - does NOT start polling when session aiSummaryStatus is "available" (terminal)
    - does NOT start polling when any status is "failed" (terminal)

    CommentThread.test.tsx (vi.mock: fetch, getConfig):
    - renders each comment in the list
    - clicking a comment row calls onSeek(comment.videoPositionMs) when onSeek prop is provided
    - clicking a comment row does nothing when onSeek prop is not provided (no error)
    - submit button is disabled when syncTime === 0
    - submit button is enabled when syncTime > 0 and text is non-empty
  </behavior>

  <implementation>
    Use the existing BroadcastPage.test.tsx as the mock pattern reference (vi.mock, MemoryRouter, waitFor).

    For HangoutPage mocks — mock at minimum:
      - useHangout: returns { localVideoRef: { current: null }, participants: [], isJoined: true, error: null, toggleMute: vi.fn(), toggleCamera: vi.fn() }
      - useChatRoom: returns { room: null, connectionState: 'disconnected', error: null }
      - useActiveSpeaker: returns { activeSpeakerId: null }
      - VideoGrid: returns <div data-testid="video-grid" />
      - ChatPanel: returns <div data-testid="chat-panel" />
      - useReactionSender: returns { sendReaction: vi.fn(), sending: false }
      - useReactionListener: vi.fn()
      - react-router-dom: mock useParams (returns { sessionId: 'test-session' }), useNavigate (returns mockNavigate)

    For VideoPage mocks:
      - useHlsPlayer: returns { videoRef: { current: null }, qualities: [], currentQuality: null, setQuality: vi.fn(), isSafari: false, syncTime: 0 }
      - fetchAuthSession: resolves with { tokens: { idToken: { toString: () => 'tok', payload: { 'cognito:username': 'user1' } } } }
      - global fetch: resolves with ok=true, json() returning a session object with the relevant status fields
      - mock child components: SessionAuditLog, VideoInfoPanel, CommentThread, SummaryDisplay, QualitySelector, ReactionSummaryPills, ReplayReactionPicker, useReactionSender

    For polling tests in VideoPage — use vi.useFakeTimers() / vi.useRealTimers() around the polling tests. Assert setInterval is called via vi.spyOn(globalThis, 'setInterval').

    For CommentThread tests:
      - mock global fetch to return { ok: true, json: () => ({ comments: [{ commentId: 'c1', sessionId: 's1', userId: 'u1', text: 'hello', videoPositionMs: 5000, createdAt: '2026-01-01' }] }) }
      - render with onSeek prop as vi.fn()
      - fireEvent.click the comment row and assert onSeek was called with 5000

    All four files must fail (RED) when run before plan 02 and 03 implementations because:
    - ConfirmDialog.test.tsx: imports non-existent file
    - HangoutPage.test.tsx: ConfirmDialog not rendered, ReactionPicker not present
    - VideoPage.test.tsx: polling setInterval not called
    - CommentThread.test.tsx: onSeek prop not wired
  </implementation>
</feature>

<verification>
After each file: `cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run --reporter=verbose 2>&1 | tail -30`

Tests must be RED (failing) — if a test passes before implementation, the test does not assert the right thing. Fix before committing.

Exception: ConfirmDialog.test.tsx will fail with "Cannot find module" — that is the expected RED state.
</verification>

<success_criteria>
- Four test files exist with failing tests
- `cd web && npx vitest run 2>&1 | tail -5` shows failures in the four new test files
- No existing passing tests are broken
</success_criteria>

<output>
After completion, create `.planning/phases/041-ui-polish-live-session-upload/041-01-SUMMARY.md`
</output>
