---
phase: 06-replay-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/handlers/list-recordings.ts
  - backend/src/repositories/session-repository.ts
  - infra/lib/stacks/api-stack.ts
  - web/src/pages/HomePage.tsx
  - web/src/features/replay/RecordingFeed.tsx
autonomous: true
requirements: [REPLAY-01, REPLAY-02, REPLAY-03]

must_haves:
  truths:
    - "User sees recently recorded sessions in home feed"
    - "User can click thumbnail to navigate to replay viewer"
    - "Home feed shows thumbnail, title, duration, broadcaster name"
  artifacts:
    - path: "backend/src/handlers/list-recordings.ts"
      provides: "GET /recordings endpoint"
      exports: ["handler"]
    - path: "backend/src/repositories/session-repository.ts"
      provides: "getRecentRecordings method"
      exports: ["getRecentRecordings"]
    - path: "web/src/features/replay/RecordingFeed.tsx"
      provides: "Recording grid component"
      min_lines: 50
    - path: "web/src/pages/HomePage.tsx"
      provides: "Home page with recording feed"
      contains: "RecordingFeed"
  key_links:
    - from: "web/src/pages/HomePage.tsx"
      to: "GET /recordings"
      via: "fetch in useEffect"
      pattern: "fetch.*recordings"
    - from: "web/src/features/replay/RecordingFeed.tsx"
      to: "/replay/:sessionId"
      via: "React Router navigate"
      pattern: "navigate.*replay"
---

<objective>
Enable users to discover recently recorded sessions on the home page with thumbnails, metadata, and navigation to replay viewer.

Purpose: Transform the home page from a sparse landing page into a content discovery feed showing available replays.
Output: Home feed displays recently recorded sessions in chronological grid with thumbnail, duration, broadcaster name, and click-to-replay navigation.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replay-viewer/06-RESEARCH.md
@.planning/phases/05-recording-foundation/05-01-SUMMARY.md
@.planning/phases/05-recording-foundation/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types executors need from existing codebase -->

From backend/src/domain/session.ts (Phase 5):
```typescript
export enum RecordingStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  AVAILABLE = 'available',
  FAILED = 'failed',
}

export interface Session {
  sessionId: string;
  userId: string;
  status: SessionStatus;
  createdAt: string;
  endedAt?: string;
  recordingS3Path?: string;
  recordingDuration?: number;      // milliseconds
  thumbnailUrl?: string;            // CloudFront URL
  recordingHlsUrl?: string;         // CloudFront URL
  recordingStatus?: RecordingStatus;
  // ... other fields
}
```

From backend/src/repositories/session-repository.ts (existing pattern):
```typescript
import { getDocumentClient } from '../lib/dynamodb-client';
import { ScanCommand, QueryCommand } from '@aws-sdk/lib-dynamodb';

export async function getSessionById(
  tableName: string,
  sessionId: string
): Promise<Session | null> {
  // Pattern: Query with PK=SESSION#{sessionId}
}
```

From web/src/App.tsx (existing routing pattern):
```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { ProtectedRoute } from './features/auth/ProtectedRoute';

<Routes>
  <Route path="/broadcast/:sessionId" element={<ProtectedRoute><BroadcastPage /></ProtectedRoute>} />
</Routes>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create List Recordings API and Repository Method</name>
  <files>
    backend/src/handlers/list-recordings.ts
    backend/src/repositories/session-repository.ts
    infra/lib/stacks/api-stack.ts
  </files>
  <action>
    Create GET /recordings Lambda handler that queries DynamoDB for recently recorded sessions.

    **Repository Method (session-repository.ts):**
    Add `export async function getRecentRecordings(tableName: string, limit: number = 20): Promise&lt;Session[]&gt;`
    - Use ScanCommand with FilterExpression: `recordingStatus = :available AND begins_with(PK, :session)`
    - ExpressionAttributeValues: { ':available': 'available', ':pk': 'SESSION#' }
    - Map Items to Session objects (strip PK/SK/entityType DynamoDB keys)
    - Filter sessions with endedAt defined
    - Sort descending by endedAt (most recent first): `new Date(b.endedAt).getTime() - new Date(a.endedAt).getTime()`
    - Return limit number of recordings (default 20)

    **Handler (list-recordings.ts):**
    Create async handler function exporting `handler`
    - Extract TABLE_NAME from process.env
    - Call getRecentRecordings(TABLE_NAME, 20)
    - Return APIGatewayProxyResult with statusCode 200, body: JSON.stringify({ recordings })
    - CORS headers: { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': '*' }
    - Error handling: catch, log, return 500 with error message

    **API Stack Integration (api-stack.ts):**
    Add GET /recordings endpoint
    - Create NodejsFunction for list-recordings handler (same pattern as existing handlers)
    - Environment variables: TABLE_NAME from sessionStack.table.tableName
    - Grant DynamoDB read permissions: sessionStack.table.grantReadData(listRecordingsFn)
    - Add API Gateway route: api.addRoutes({ path: '/recordings', methods: [apigw.HttpMethod.GET], integration: new integrations.HttpLambdaIntegration(...) })
    - NO authorizer (public endpoint for discovery)

    Pattern follows existing handlers (e.g., start-broadcast.ts, get-playback.ts).
    Use @aws-sdk/lib-dynamodb (already installed).
  </action>
  <verify>
    <automated>
      # Build verification
      cd /Users/connorhoehn/Projects/videonowandlater/backend && npm run build

      # Type check
      cd /Users/connorhoehn/Projects/videonowandlater/infra && npm run build

      # Repository export check
      grep -q "export async function getRecentRecordings" /Users/connorhoehn/Projects/videonowandlater/backend/src/repositories/session-repository.ts

      # Handler export check
      grep -q "export async function handler" /Users/connorhoehn/Projects/videonowandlater/backend/src/handlers/list-recordings.ts

      # API route check
      grep -q "path: '/recordings'" /Users/connorhoehn/Projects/videonowandlater/infra/lib/stacks/api-stack.ts
    </automated>
  </verify>
  <done>
    GET /recordings endpoint deployed, returns Session[] with recordingStatus='available' sorted by endedAt descending, repository method tested with type-check build, API stack compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecordingFeed Component and Update HomePage</name>
  <files>
    web/src/features/replay/RecordingFeed.tsx
    web/src/pages/HomePage.tsx
  </files>
  <action>
    Create RecordingFeed component displaying recently recorded sessions in chronological grid.

    **RecordingFeed Component (web/src/features/replay/RecordingFeed.tsx):**
    Create React component accepting `recordings: Recording[]` prop
    - Define Recording interface: { sessionId, thumbnailUrl, recordingDuration (ms), createdAt, userId }
    - Grid layout: `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4 p-4` (Tailwind, responsive)
    - Map over recordings, render card for each:
      - Thumbnail: `&lt;img src={recording.thumbnailUrl} alt="Recording thumbnail" className="w-full h-full object-cover" /&gt;`
      - Container: `aspect-video bg-gray-200 rounded-lg overflow-hidden relative`
      - Duration badge: Positioned absolute bottom-2 right-2, `bg-black bg-opacity-75 text-white text-xs px-2 py-1 rounded`
        - Format duration: `const formatDuration = (ms) =&gt; { const min = Math.floor(ms / 60000); const sec = Math.floor((ms % 60000) / 1000); return min + ':' + sec.toString().padStart(2, '0'); }`
      - Metadata: Display createdAt as date, userId as broadcaster name (simple for v1.1)
      - Click handler: `onClick={() =&gt; navigate('/replay/' + recording.sessionId)}`
      - Hover effect: `group-hover:scale-105 transition` on thumbnail
    - Use `useNavigate()` from react-router-dom for navigation
    - Handle empty state: "No recordings yet" message if recordings.length === 0

    **HomePage Update (web/src/pages/HomePage.tsx):**
    Extend existing HomePage to fetch and display recordings
    - Import RecordingFeed component
    - Add state: `const [recordings, setRecordings] = useState&lt;Recording[]&gt;([])`
    - Add loading state: `const [loading, setLoading] = useState(true)`
    - useEffect on mount:
      - Fetch GET /recordings from API (use existing API_URL pattern from other pages)
      - Parse response, setRecordings(data.recordings)
      - setLoading(false)
      - Error handling: log to console, show error state
    - Render loading spinner while loading (existing pattern from other pages)
    - Render RecordingFeed component with recordings prop
    - Keep existing "Get Started" content ABOVE feed (recordings appear below)

    Match existing Tailwind patterns from BroadcastPage.tsx and ViewerPage.tsx (Phase 3 Plan 2).
    Use existing useNavigate pattern from app routing.
  </action>
  <verify>
    <automated>
      # Build verification
      cd /Users/connorhoehn/Projects/videonowandlater/web && npm run build

      # Component exports check
      grep -q "export.*RecordingFeed" /Users/connorhoehn/Projects/videonowandlater/web/src/features/replay/RecordingFeed.tsx

      # Navigation check
      grep -q "navigate.*replay" /Users/connorhoehn/Projects/videonowandlater/web/src/features/replay/RecordingFeed.tsx

      # HomePage integration check
      grep -q "RecordingFeed" /Users/connorhoehn/Projects/videonowandlater/web/src/pages/HomePage.tsx

      # Fetch check
      grep -q "fetch.*recordings" /Users/connorhoehn/Projects/videonowandlater/web/src/pages/HomePage.tsx
    </automated>
  </verify>
  <done>
    HomePage displays recently recorded sessions in responsive grid, clicking thumbnail navigates to /replay/:sessionId, empty state handled, build succeeds with no TypeScript errors.
  </done>
</task>

</tasks>

<verification>
**API Verification:**
- Deploy stack: `cd infra && npm run cdk deploy`
- Manual API test: `curl https://{api-domain}/recordings` returns JSON array with recordings

**Frontend Verification:**
- Visual test: Navigate to home page, verify grid displays with thumbnails
- Click test: Click recording thumbnail, verify navigation to /replay/:sessionId
- Empty state: Verify message if no recordings exist
- Responsive: Check mobile (1 column), tablet (2 columns), desktop (3 columns)
</verification>

<success_criteria>
1. GET /recordings endpoint returns Session[] with recordingStatus='available' sorted by endedAt
2. RecordingFeed displays grid of recordings with thumbnails, duration badges, metadata
3. Clicking recording navigates to /replay/:sessionId route (route created in Plan 06-02)
4. HomePage shows recording feed below existing content
5. Build succeeds for backend, infra, and web packages
</success_criteria>

<output>
After completion, create `.planning/phases/06-replay-viewer/06-01-SUMMARY.md`
</output>
