---
phase: 08-realtime-hangouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/handlers/join-hangout.ts
  - backend/src/repositories/session-repository.ts
  - infra/lib/stacks/api-stack.ts
autonomous: true
requirements: [HANG-02, HANG-03, HANG-04]

must_haves:
  truths:
    - "Server generates participant tokens with PUBLISH+SUBSCRIBE capabilities and 12-hour TTL"
    - "Participant tokens include userId in attributes for audit trail"
    - "Pre-warmed Stage pool already exists from Phase 2 with recording configuration attached"
  artifacts:
    - path: "backend/src/handlers/join-hangout.ts"
      provides: "Participant token generation Lambda"
      exports: ["handler"]
    - path: "backend/src/repositories/session-repository.ts"
      provides: "findSessionByStageArn query function"
      exports: ["findSessionByStageArn"]
  key_links:
    - from: "backend/src/handlers/join-hangout.ts"
      to: "@aws-sdk/client-ivs-realtime"
      via: "CreateParticipantTokenCommand"
      pattern: "CreateParticipantTokenCommand"
    - from: "infra/lib/stacks/api-stack.ts"
      to: "backend/src/handlers/join-hangout.ts"
      via: "Lambda integration on POST /sessions/{sessionId}/join"
      pattern: "addRoutes.*join"
---

<objective>
Create server-side participant token generation for RealTime hangouts with secure capability control and session validation.

Purpose: Enable users to join hangout sessions via server-generated tokens (not exposed Stage ARNs), with fine-grained capabilities (PUBLISH, SUBSCRIBE) and audit trail (userId in token attributes).

Output: Lambda handler that generates IVS RealTime participant tokens, API endpoint for token exchange, and repository query function for Stage ARN-based session lookup.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-realtime-hangouts/08-RESEARCH.md

# Prior phase patterns
@.planning/phases/02-session-model-and-resource-pool/02-02-SUMMARY.md
@.planning/phases/05-recording-foundation/05-02-SUMMARY.md

# Existing code
@backend/src/domain/session.ts
@backend/src/handlers/replenish-pool.ts (lines 1-82 show Stage pool pattern)
@infra/lib/stacks/api-stack.ts (reference for route wiring pattern)
</context>

<interfaces>
<!-- Existing Session type from backend/src/domain/session.ts -->
```typescript
export interface Session {
  sessionId: string;
  userId: string;
  sessionType: SessionType;  // 'BROADCAST' | 'HANGOUT'
  status: SessionStatus;
  claimedResources: ClaimedResources;
  createdAt: string;
  startedAt?: string;
  endedAt?: string;
  version: number;
  recordingS3Path?: string;
  recordingDuration?: number;
  thumbnailUrl?: string;
  recordingHlsUrl?: string;
  recordingStatus?: RecordingStatus;
}

export interface ClaimedResources {
  channel?: string;  // IVS Channel ARN for broadcasts
  stage?: string;    // IVS Stage ARN for hangouts
  chatRoom: string;  // IVS Chat Room ARN
}
```

<!-- IVS RealTime SDK from research -->
```typescript
// From @aws-sdk/client-ivs-realtime
import { CreateParticipantTokenCommand } from '@aws-sdk/client-ivs-realtime';

interface CreateParticipantTokenCommandInput {
  stageArn: string;
  duration?: number;  // TTL in seconds
  userId: string;     // Participant identifier for events
  capabilities?: ('PUBLISH' | 'SUBSCRIBE')[];
  attributes?: Record<string, string>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create join-hangout handler with participant token generation</name>
  <files>backend/src/handlers/join-hangout.ts, backend/src/handlers/__tests__/join-hangout.test.ts</files>
  <behavior>
    Test 1: Handler validates sessionId and userId presence (400 if missing)
    Test 2: Handler returns 404 if session not found or sessionType != HANGOUT
    Test 3: Handler generates token with userId, capabilities:[PUBLISH,SUBSCRIBE], 12-hour TTL
    Test 4: Handler returns token structure: {token, participantId, expirationTime}
  </behavior>
  <action>
    Create Lambda handler that:
    1. Extracts sessionId from path parameters and userId from Cognito authorizer claims (event.requestContext.authorizer.claims.sub)
    2. Validates both parameters present, returns 400 if missing
    3. Fetches session from DynamoDB using existing getSessionById function
    4. Validates session exists and sessionType === 'HANGOUT', returns 404 otherwise
    5. Extracts Stage ARN from session.claimedResources.stage
    6. Calls CreateParticipantTokenCommand with:
       - stageArn from session
       - duration: 43200 (12 hours in seconds)
       - userId from Cognito claims
       - capabilities: ['PUBLISH', 'SUBSCRIBE']
       - attributes: { username: event.requestContext.authorizer.claims['cognito:username'] || 'Anonymous' }
    7. Returns 200 with JSON: {token: response.participantToken.token, participantId: response.participantToken.participantId, expirationTime: response.participantToken.expirationTime}
    8. Error handling: catch and return 500 with error message

    Use getIVSRealTimeClient() singleton pattern from existing lib/ivs-clients.ts (already supports IVSRealTimeClient).
  </action>
  <verify>
    <automated>npm test -- join-hangout.test.ts -x</automated>
  </verify>
  <done>Handler exists, tests pass (4 test cases), TypeScript compiles, returns participant tokens for valid HANGOUT sessions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend session repository with findSessionByStageArn query</name>
  <files>backend/src/repositories/session-repository.ts, backend/src/repositories/__tests__/session-repository.test.ts</files>
  <behavior>
    Test 1: findSessionByStageArn returns session when claimedResources.stage matches
    Test 2: findSessionByStageArn returns null when no matching Stage ARN found
    Test 3: Query uses Scan with FilterExpression (no GSI for Stage ARN lookup)
  </behavior>
  <action>
    Extend session-repository.ts with:
    1. Export async function findSessionByStageArn(tableName: string, stageArn: string): Promise&lt;Session | null&gt;
    2. Use DynamoDB Scan (no index available for claimedResources.stage):
       - TableName: tableName
       - FilterExpression: 'begins_with(PK, :pkPrefix) AND claimedResources.stage = :stageArn'
       - ExpressionAttributeValues: { ':pkPrefix': 'SESSION#', ':stageArn': stageArn }
    3. Return first matching item as Session (unmarshal from DynamoDB format), or null if no match
    4. Import Session type from domain/session
    5. Add unit tests to existing session-repository.test.ts

    Note: Scan is acceptable here because this query only runs in recording-ended handler (low frequency). If needed for high-frequency queries in future, can add GSI3 with claimedResources.stage as key.

    This function will be used by recording-ended handler (Plan 08-03) to find hangout sessions by Stage ARN when EventBridge recording events arrive.
  </action>
  <verify>
    <automated>npm test -- session-repository.test.ts -x</automated>
  </verify>
  <done>findSessionByStageArn function exported, tests pass (3 test cases), query returns sessions by Stage ARN</done>
</task>

<task type="auto">
  <name>Task 3: Wire join-hangout handler to API with POST /sessions/:sessionId/join route</name>
  <files>infra/lib/stacks/api-stack.ts</files>
  <action>
    In api-stack.ts:
    1. Create NodejsFunction for join-hangout handler:
       - runtime: lambda.Runtime.NODEJS_20_X
       - handler: 'handler'
       - entry: path.join(__dirname, '../../../backend/src/handlers/join-hangout.ts')
       - timeout: Duration.seconds(10)
       - environment: { TABLE_NAME: sessionStack.table.tableName }
    2. Grant DynamoDB read permissions: sessionStack.table.grantReadData(joinHangoutFn)
    3. Add IAM policy for CreateParticipantToken:
       joinHangoutFn.addToRolePolicy(new iam.PolicyStatement({
         actions: ['ivs:CreateParticipantToken'],
         resources: ['*'], // IVS doesn't support resource-level permissions for CreateParticipantToken
       }))
    4. Create Lambda integration: new HttpLambdaIntegration('JoinHangoutIntegration', joinHangoutFn)
    5. Add route to httpApi:
       httpApi.addRoutes({
         path: '/sessions/{sessionId}/join',
         methods: [HttpMethod.POST],
         integration: joinHangoutIntegration,
         authorizer: jwtAuthorizer, // Require authentication (same as other protected routes)
       })
  </action>
  <verify>npx cdk synth VNL-Api --quiet && echo "CDK synth successful" && grep -q "JoinHangout" cdk.out/VNL-Api.template.json && echo "JoinHangout Lambda found in template"</verify>
  <done>API route /sessions/:sessionId/join created, Lambda wired with DynamoDB read and CreateParticipantToken permissions, CDK synth produces valid CloudFormation</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Backend tests pass: `cd backend && npm test -- join-hangout -x`
2. Session repository tests pass: `cd backend && npm test -- session-repository -x`
3. TypeScript compiles: `cd backend && npx tsc --noEmit`
4. CDK synth succeeds: `npx cdk synth VNL-Api`
5. API route exists in template: `grep "/sessions/{sessionId}/join" cdk.out/VNL-Api.template.json`
6. IAM policy includes ivs:CreateParticipantToken: `grep "ivs:CreateParticipantToken" cdk.out/VNL-Api.template.json`
</verification>

<success_criteria>
- join-hangout.ts Lambda handler generates participant tokens with PUBLISH+SUBSCRIBE capabilities and 12-hour TTL
- findSessionByStageArn repository function queries sessions by Stage ARN using Scan
- POST /sessions/:sessionId/join API route authenticated with Cognito JWT authorizer
- All backend unit tests pass (join-hangout, session-repository)
- CDK synth produces valid CloudFormation with Lambda, API route, and IAM permissions
</success_criteria>

<output>
After completion, create `.planning/phases/08-realtime-hangouts/08-01-SUMMARY.md`
</output>
