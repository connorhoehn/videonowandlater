---
phase: 09.1-typescript-build-fixes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
autonomous: true
requirements: [HANG-05, HANG-06, HANG-07, HANG-08, HANG-09, HANG-10, HANG-11, REACT-01, REACT-07]
gap_closure: true

must_haves:
  truths:
    - "cd backend && npm test -- --testPathPattern=stream-started exits with code 0"
    - "No 'dynamic import callback was invoked without --experimental-vm-modules' error during backend jest runs"
    - "Both test and test:watch scripts in backend/package.json prefix NODE_OPTIONS=--experimental-vm-modules"
  artifacts:
    - path: "backend/package.json"
      provides: "Backend test scripts with --experimental-vm-modules Node flag"
      contains: "NODE_OPTIONS=--experimental-vm-modules"
  key_links:
    - from: "backend/package.json test script"
      to: "Jest runtime"
      via: "NODE_OPTIONS=--experimental-vm-modules"
      pattern: "NODE_OPTIONS.*experimental-vm-modules"
---

<objective>
Enable the backend Jest test suite to execute AWS SDK v3 handler tests successfully by setting the --experimental-vm-modules Node.js flag.

Purpose: The single remaining gap from Phase 09.1 verification is that `npm test -- --testPathPattern=stream-started` fails at runtime with "TypeError: A dynamic import callback was invoked without --experimental-vm-modules". The TypeScript compile errors were fully fixed in Plans 01-02. This gap is a Jest + AWS SDK v3 ESM infrastructure incompatibility — Jest requires the --experimental-vm-modules flag to handle the ESM dynamic imports used internally by AWS SDK v3 packages. The fix is a one-line update to backend/package.json test scripts.

Output: backend/package.json test scripts with NODE_OPTIONS=--experimental-vm-modules prefix, enabling all 6 affected handler test suites to run.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-typescript-build-fixes/09.1-02-SUMMARY.md

<interfaces>
<!-- Current backend/package.json scripts section (extracted from codebase) -->
<!-- Executor should update these directly — no codebase exploration needed. -->

Current backend/package.json scripts:
```json
"scripts": {
  "build": "tsc",
  "test": "jest",
  "test:watch": "jest --watch"
}
```

Current backend/jest.config.js:
```js
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.test.ts'],
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.test.ts',
  ],
};
```

Gap from VERIFICATION.md:
- Truth: "Running `cd backend && npx jest --testPathPatterns=stream-started` exits with code 0"
- Status: FAILED
- Reason: "Tests compile cleanly (TS2554 fixed) but fail at runtime with 'TypeError: A dynamic import callback was invoked without --experimental-vm-modules'. Pre-existing Jest + AWS SDK v3 ESM incompatibility affecting 6 handler test suites."
- Missing: "Add NODE_OPTIONS='--experimental-vm-modules' to backend jest invocation (e.g., in jest.config.js or package.json test script)"

Environment: Node v20.19.2, Jest v30.1.3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --experimental-vm-modules to backend test scripts</name>
  <files>backend/package.json</files>
  <action>
    Update the test and test:watch scripts in backend/package.json to prefix NODE_OPTIONS=--experimental-vm-modules before the jest invocation.

    Change the scripts section from:
    ```json
    "scripts": {
      "build": "tsc",
      "test": "jest",
      "test:watch": "jest --watch"
    }
    ```

    To:
    ```json
    "scripts": {
      "build": "tsc",
      "test": "NODE_OPTIONS=--experimental-vm-modules jest",
      "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch"
    }
    ```

    Do NOT modify jest.config.js — the NODE_OPTIONS env var approach is the correct solution for Jest + AWS SDK v3 ESM compatibility. Do NOT add "type": "module" to package.json — the backend uses NodeNext module resolution via tsconfig, not native ESM package mode; adding "type":"module" would break CommonJS interop in the Lambda build output.

    Reason for NODE_OPTIONS approach over jest.config.js extensionsToTreatAsEsm: jest.config.js uses CommonJS module.exports format (not ESM), and the ESM transformation needed is at the Node runtime level, not the Jest transform level. AWS SDK v3 uses dynamic import() internally which requires the experimental flag in Node versions prior to stabilization.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/backend && npm test -- --testPathPattern=stream-started --forceExit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - backend/package.json test script reads "NODE_OPTIONS=--experimental-vm-modules jest"
    - backend/package.json test:watch script reads "NODE_OPTIONS=--experimental-vm-modules jest --watch"
    - npm test -- --testPathPattern=stream-started --forceExit exits with code 0 (no "experimental-vm-modules" error in output)
    - Backend build (npm run build) still exits with code 0 (build script unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all affected handler test suites pass</name>
  <files></files>
  <action>
    Run the full backend test suite to confirm the --experimental-vm-modules fix resolves the ESM incompatibility across all 6 affected handler test suites (stream-started, replenish-pool, get-viewer-count, recording-ended, get-playback, start-broadcast). This is a verification-only task — no file changes.

    Run: cd backend && npm test -- --forceExit 2>&1

    If any test suite fails for reasons OTHER than the vm-modules error (i.e., genuine test logic failures unrelated to ESM infrastructure), document them in the SUMMARY but do not attempt to fix them in this plan — they are out of scope for Phase 09.1 gap closure.

    Do NOT attempt to fix Jest test logic failures, missing test fixtures, or AWS SDK mock configuration issues — those are separate concerns outside Phase 09.1 scope.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/backend && npm test -- --forceExit 2>&1 | grep -E "(Tests:|Test Suites:|PASS|FAIL|experimental-vm-modules)" | head -20</automated>
  </verify>
  <done>
    - No test suite fails with "experimental-vm-modules" error (confirms the gap is closed)
    - Output shows test suites executing (PASS or FAIL for logic reasons, not ESM infrastructure errors)
    - Result documented in SUMMARY regardless of individual test pass/fail status
  </done>
</task>

</tasks>

<verification>
Gap closure verified when:
1. cd backend && npm test -- --testPathPattern=stream-started --forceExit exits with code 0
2. The string "experimental-vm-modules" does NOT appear as an error in test output
3. backend/package.json diff shows NODE_OPTIONS=--experimental-vm-modules in both test scripts
4. cd backend && npm run build still exits with code 0 (build unaffected)
</verification>

<success_criteria>
- VERIFICATION.md gap truth "exits with code 0" is now satisfied
- Phase 09.1 achieves 7/7 must-have truths verified
- Zero new TypeScript errors introduced
- Backend test infrastructure works for all handler test suites
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-typescript-build-fixes/09.1-03-SUMMARY.md` following the summary template. Include:
- The exact scripts change made to backend/package.json
- Test output confirming stream-started suite passes
- Note on which of the 6 affected suites pass vs. fail (and why, if any fail for non-infrastructure reasons)
- Confirmation that the Phase 09.1 gap is closed
</output>
