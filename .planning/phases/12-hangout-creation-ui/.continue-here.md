---
phase: ad-hoc-bug-fixes (session 2026-03-04)
task: complete
total_tasks: complete
status: in_progress
last_updated: 2026-03-04T15:33:16.181Z
---

<current_state>
Ad-hoc debugging session fixing live broadcast pipeline bugs. All code changes deployed.
The IAM fix is the last piece — StartBroadcastHandler now has UpdateItem permission.
A new broadcast test is needed to confirm goLive: true works end-to-end.
</current_state>

<completed_work>

- IVS Chat token: tokenProvider was returning `data.token` (string) instead of `data` (ChatToken object) → fixed in `useChatRoom.ts:28`
- Duplicate ChatRoom instances: `ChatPanel` was calling `useChatRoom` internally AND `BroadcastPage` was also calling it → fixed by making ChatPanel use `useChatRoomContext()`, added `connectionState` prop
- ViewerPage now creates room at page level and wraps with ChatRoomProvider
- API URL bug: `ChatMessagesProvider`, `useReactionSender`, `ReplayChat` all used `window.APP_CONFIG?.apiBaseUrl` (undefined in dev) → fixed to use `getConfig()?.apiUrl`
- `stream-started.ts`: was using `event.detail.channel_name` (display name) instead of `event.detail.channel_arn` (full ARN) → scan never found session → session never became LIVE → both "Session must be live" and "has no startedAt" errors cascaded from this
- `getRecentRecordings`: changed from filtering `recordingStatus='available'` to `status='ended'` so sessions appear immediately after broadcast ends with status overlays
- `RecordingFeed`: added `recordingStatus` field and overlays (Processing..., Awaiting recording..., Processing failed)
- `start-broadcast.ts`: added `goLive: true` body flag support + explicit LIVE transition via `updateSessionStatus`
- `useBroadcast.ts`: passes `{ goLive: true }` in the startBroadcast fetch call
- IAM fix: `StartBroadcastHandler` had `grantReadData` → changed to `grantReadWriteData` in `api-stack.ts` (root cause of goLive silently failing — no `dynamodb:UpdateItem` permission)
- Created `scripts/test-live-endpoints.sh` — tests reactions + messages, offers to force LIVE
- Created `scripts/cleanup-resources.sh` — resets stale sessions + pool channels/chats back to AVAILABLE
- All changes deployed via `bash scripts/deploy.sh`

</completed_work>

<remaining_work>

- Verify goLive: true works on a fresh broadcast (IAM fix just deployed — needs a new session test)
- Run `./scripts/cleanup-resources.sh` to reset all stale CREATING sessions and reclaim pool resources
- Confirm recording appears on home page after stopping a broadcast (the home page now shows ENDED sessions with recordingStatus overlays)
- Phase 12 (hangout-creation-ui) — not yet planned/executed

</remaining_work>

<decisions_made>

- goLive: true approach: rather than relying solely on EventBridge stream-started event (which had channel_name/channel_arn bug), the frontend now explicitly calls /start with goLive:true to immediately set LIVE status. EventBridge is now a redundant fallback.
- session-repository getRecentRecordings: changed filter from recordingStatus=available to status=ended so sessions show on home page while recording processes
- RecordingFeed: non-available recordings render with status overlay and are non-clickable

</decisions_made>

<blockers>

- None currently — all fixes deployed. Just needs a live test.

</blockers>

<context>
Session was entirely ad-hoc bug fixing on the live broadcast pipeline. No formal GSD phase.
The core issue chain was: EventBridge bug (channel_name vs channel_arn) → session never LIVE → everything downstream failed.
Added goLive:true as a more reliable synchronous path. IAM permission was the final missing piece.
</context>

<next_action>
1. Run `./scripts/cleanup-resources.sh` to reset pool resources
2. Start a fresh broadcast to test goLive: true works (session should transition to LIVE, chat/reactions should persist)
3. Stop broadcast and check home page for the recording with status overlay
4. Then plan/execute Phase 12 (hangout-creation-ui) via /gsd:plan-phase
</next_action>
