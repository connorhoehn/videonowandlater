---
phase: 18-homepage-redesign-activity-feed
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - web/src/features/activity/ReactionSummaryPills.tsx
  - web/src/features/activity/RecordingSlider.tsx
  - web/src/features/activity/ActivityFeed.tsx
  - web/src/features/activity/BroadcastActivityCard.tsx
  - web/src/features/activity/HangoutActivityCard.tsx
  - web/src/features/activity/__tests__/ReactionSummaryPills.test.tsx
  - web/src/features/activity/__tests__/RecordingSlider.test.tsx
  - web/src/features/activity/__tests__/ActivityFeed.test.tsx
  - web/src/features/activity/__tests__/BroadcastActivityCard.test.tsx
  - web/src/features/activity/__tests__/HangoutActivityCard.test.tsx
  - web/src/pages/HomePage.tsx
autonomous: true
requirements:
  - ACTV-01
  - ACTV-02
  - ACTV-03
  - ACTV-04
  - ACTV-05
must_haves:
  truths:
    - "HomePage displays a horizontal recording slider showing 3-4 broadcast cards with peek-scrolling"
    - "Recording slider filters out hangout sessions (broadcasts only)"
    - "Below the slider, an activity feed displays all recent sessions in reverse chronological order"
    - "Broadcast activity cards display userId, duration, reaction summary pills, and relative timestamp"
    - "Hangout activity cards display participant list, message count, duration, and relative timestamp"
    - "Reaction summary pills show emoji + count for each reaction type"
  artifacts:
    - path: web/src/features/activity/ReactionSummaryPills.tsx
      provides: "Reusable emoji + count pill component"
      exports: ["ReactionSummaryPills"]
    - path: web/src/features/activity/RecordingSlider.tsx
      provides: "Horizontal scrollable recording slider with CSS scroll-snap (broadcasts only)"
      exports: ["RecordingSlider"]
    - path: web/src/features/activity/ActivityFeed.tsx
      provides: "Vertical activity feed listing all recent sessions in reverse chronological order"
      exports: ["ActivityFeed"]
    - path: web/src/features/activity/BroadcastActivityCard.tsx
      provides: "Broadcast activity card with title, duration, reactions, timestamp"
      exports: ["BroadcastActivityCard"]
    - path: web/src/features/activity/HangoutActivityCard.tsx
      provides: "Hangout activity card with participant list, message count, duration, timestamp"
      exports: ["HangoutActivityCard"]
    - path: web/src/pages/HomePage.tsx
      provides: "Updated homepage layout with recording slider + activity feed"
      contains: "RecordingSlider.*ActivityFeed"
  key_links:
    - from: HomePage.tsx
      to: "GET /activity endpoint"
      via: "fetch in useEffect"
      pattern: "fetch.*api/activity"
    - from: RecordingSlider.tsx
      to: "sessionType === BROADCAST filter"
      via: "filter in render"
      pattern: "sessionType.*BROADCAST"
    - from: BroadcastActivityCard.tsx
      to: ReactionSummaryPills
      via: "import and render"
      pattern: "ReactionSummaryPills"
---

<objective>
Redesign the homepage with a two-zone layout: a horizontal recording slider (broadcasts only) and an activity feed below (all sessions). Create reusable activity card components that display rich metadata (reactions, participants, message counts, timestamps).

Purpose: Shift from a simple recording grid to a more discoverable two-zone layout that surfaces broadcast recordings and hangout activity with contextual metadata.

Output: ReactionSummaryPills, RecordingSlider, ActivityFeed, BroadcastActivityCard, HangoutActivityCard components, updated HomePage, and comprehensive component tests.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-homepage-redesign-activity-feed/18-RESEARCH.md
@.planning/phases/18-homepage-redesign-activity-feed/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReactionSummaryPills and RecordingSlider components</name>
  <files>web/src/features/activity/ReactionSummaryPills.tsx, web/src/features/activity/RecordingSlider.tsx, web/src/features/activity/__tests__/ReactionSummaryPills.test.tsx, web/src/features/activity/__tests__/RecordingSlider.test.tsx</files>
  <action>
Create ReactionSummaryPills.tsx:
1. Accept prop: reactionSummary?: Record<string, number>
2. Import EMOJI_MAP from web/src/features/reactions/ReactionPicker.tsx
3. Render empty state: "No reactions" if empty or undefined
4. For each emoji type, render a pill: <div className="flex items-center gap-1 px-2 py-1 bg-gray-100 rounded-full"><emoji> <count></div>
5. Tests: (1) renders emoji pills, (2) handles empty summary, (3) displays correct counts

Create RecordingSlider.tsx:
1. Accept prop: sessions: ActivitySession[]
2. Filter to broadcasts only: sessions.filter(s => s.sessionType === 'BROADCAST')
3. Horizontal scrollable container with CSS scroll-snap: className="overflow-x-auto snap-x snap-mandatory scroll-smooth"
4. Cards: className="snap-center flex-shrink-0 w-56" (3.5 cards visible with peek)
5. Each card shows: thumbnail, userId, reaction pills (using ReactionSummaryPills)
6. Cards navigate to /replay/:sessionId on click
7. Tests: (1) filters broadcasts only, (2) snap-mandatory class present, (3) navigates on click, (4) empty list handling
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npm test -- --testNamePattern="ReactionSummaryPills|RecordingSlider" 2>&1 | grep -E "PASS|FAIL|Tests:" | head -10</automated>
  </verify>
  <done>ReactionSummaryPills renders emoji pills; RecordingSlider displays broadcasts with scroll-snap CSS; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create ActivityFeed and activity card components</name>
  <files>web/src/features/activity/ActivityFeed.tsx, web/src/features/activity/BroadcastActivityCard.tsx, web/src/features/activity/HangoutActivityCard.tsx, web/src/features/activity/__tests__/ActivityFeed.test.tsx, web/src/features/activity/__tests__/BroadcastActivityCard.test.tsx, web/src/features/activity/__tests__/HangoutActivityCard.test.tsx</files>
  <action>
Create ActivityFeed.tsx:
1. Accept prop: sessions: ActivitySession[]
2. Sort sessions by endedAt DESC (most recent first)
3. Render vertical stack: <div className="space-y-4">
4. For each session: render BroadcastActivityCard if sessionType === 'BROADCAST', else HangoutActivityCard
5. Empty state: "No activity yet" if sessions is empty
6. Tests: (1) sorts DESC by endedAt, (2) renders broadcast cards, (3) renders hangout cards, (4) empty state

Create BroadcastActivityCard.tsx:
1. Accept prop: session: ActivitySession (broadcast type)
2. Display: userId (title), recordingDuration formatted as MM:SS, reaction pills (ReactionSummaryPills), relative timestamp
3. Helper: formatDuration(ms: number): string returns "MM:SS"
4. Helper: formatDate(dateString: string): string returns "just now", "2h", "3d", etc.
5. Clickable to /replay/:sessionId
6. Tests: (1) renders all fields, (2) formats duration correctly, (3) formats timestamp correctly, (4) navigates on click

Create HangoutActivityCard.tsx:
1. Accept prop: session: ActivitySession (hangout type)
2. Display: userId, participantCount, messageCount, recordingDuration, relative timestamp
3. Format participants as "User1, User2, +1 more" (if available)
4. Clickable to /replay/:sessionId
5. Tests: (1) renders all fields, (2) formats duration correctly, (3) formats timestamp correctly, (4) navigates on click
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npm test -- --testNamePattern="ActivityFeed|BroadcastActivityCard|HangoutActivityCard" 2>&1 | grep -E "PASS|FAIL|Tests:" | head -15</automated>
  </verify>
  <done>ActivityFeed sorts sessions DESC; BroadcastActivityCard and HangoutActivityCard render all metadata; all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Update HomePage.tsx to use new layout and fetch from GET /activity</name>
  <files>web/src/pages/HomePage.tsx</files>
  <action>
Update HomePage.tsx:
1. Replace current RecordingFeed grid with two-zone layout: RecordingSlider + ActivityFeed
2. In useEffect, fetch from GET /activity (public endpoint, no auth needed)
   - Optional: Add Authorization header if available (for consistency), but endpoint is public
3. Parse response: { sessions: ActivitySession[] }
4. Store in useState: [sessions, setSessions]
5. Render:
   - <RecordingSlider sessions={sessions} />
   - <ActivityFeed sessions={sessions} />
6. Loading state: Show spinner while fetching
7. Error state: Show "Failed to load activity" message
8. After fetch succeeds, both slider and feed populate from same data source (single API call)

Reference existing patterns:
- RecordingFeed.tsx for useEffect + useState pattern
- RecordingFeed.tsx for loading/error states
- HomePage.tsx structure for page layout
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npm run build 2>&1 | grep -E "error|Error|TypeError|FAIL" | head -10 || echo "Build succeeded"</automated>
  </verify>
  <done>HomePage.tsx imports RecordingSlider and ActivityFeed, fetches from GET /activity, renders two-zone layout; web builds without errors</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Frontend tests pass:** `cd web && npm test -- --testNamePattern="activity|Activity|Recording" 2>&1 | tail -5`
2. **Web builds successfully:** `cd web && npm run build 2>&1 | grep -i success`
3. **HomePage renders:** Load /homepage in browser, see slider + feed layout
4. **RecordingSlider filters broadcasts:** Verify only broadcasts appear in slider, no hangouts
5. **ActivityFeed shows all sessions:** Verify both broadcasts and hangouts appear in feed below slider
6. **Reaction pills display:** Verify emoji + count pills visible on broadcast cards in both slider and feed
7. **Activity cards clickable:** Verify clicking any card navigates to /replay/:id
</verification>

<success_criteria>
- HomePage displays two-zone layout: horizontal slider + activity feed below
- Recording slider shows 3-4 broadcast cards with CSS scroll-snap
- Recording slider filters out hangout sessions
- Activity feed displays all recent sessions in reverse chronological order
- Broadcast activity cards show title, duration, reaction pills, and timestamp
- Hangout activity cards show participant count, message count, duration, and timestamp
- All components tested and passing
- Web builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-homepage-redesign-activity-feed/18-02-SUMMARY.md` with:
- Frontend layout redesigned with two-zone structure
- ReactionSummaryPills, RecordingSlider, ActivityFeed, BroadcastActivityCard, HangoutActivityCard components implemented and tested
- HomePage.tsx updated to fetch from GET /activity and render new layout
- All frontend tests passing
</output>
