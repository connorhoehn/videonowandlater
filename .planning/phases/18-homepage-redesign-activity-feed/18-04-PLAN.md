---
phase: 18-homepage-redesign-activity-feed
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/features/activity/__tests__/ReactionSummaryPills.test.tsx
  - web/src/features/activity/__tests__/RecordingSlider.test.tsx
  - web/src/features/activity/__tests__/ActivityFeed.test.tsx
  - web/src/features/activity/__tests__/BroadcastActivityCard.test.tsx
  - web/src/features/activity/__tests__/HangoutActivityCard.test.tsx
autonomous: true
gap_closure: true
requirements:
  - RSUMM-02
  - RSUMM-03
  - ACTV-01
  - ACTV-02
  - ACTV-03
  - ACTV-04
  - ACTV-05
  - ACTV-06

must_haves:
  truths:
    - "All 5 activity component test files declared in plan 18-02 exist on disk"
    - "All tests pass via vitest"
  artifacts:
    - path: "web/src/features/activity/__tests__/ReactionSummaryPills.test.tsx"
      provides: "Tests for emoji pill rendering and empty state"
    - path: "web/src/features/activity/__tests__/RecordingSlider.test.tsx"
      provides: "Tests for broadcast-only filter, scroll-snap container, card rendering, empty state, navigation"
    - path: "web/src/features/activity/__tests__/ActivityFeed.test.tsx"
      provides: "Tests for reverse chronological sort, card type dispatch, empty state"
    - path: "web/src/features/activity/__tests__/BroadcastActivityCard.test.tsx"
      provides: "Tests for userId, duration format, reaction pills, relative timestamp, click navigation"
    - path: "web/src/features/activity/__tests__/HangoutActivityCard.test.tsx"
      provides: "Tests for userId, participantCount, messageCount, plural handling, duration, click navigation"
  key_links:
    - from: "web/src/features/activity/__tests__/RecordingSlider.test.tsx"
      to: "web/src/features/activity/RecordingSlider.tsx"
      via: "import and render"
      pattern: "import.*RecordingSlider"
    - from: "web/src/features/activity/__tests__/ActivityFeed.test.tsx"
      to: "web/src/features/activity/ActivityFeed.tsx"
      via: "import and render"
      pattern: "import.*ActivityFeed"
---

<objective>
Create the 5 missing frontend component test files that were declared in plan 18-02's files_modified but never created during execution. The `web/src/features/activity/__tests__/` directory exists but is empty.

Purpose: Close the single gap identified in 18-VERIFICATION.md -- all declared plan artifacts must exist on disk.
Output: 5 vitest test files covering ReactionSummaryPills, RecordingSlider, ActivityFeed, BroadcastActivityCard, HangoutActivityCard.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-homepage-redesign-activity-feed/18-VERIFICATION.md

<interfaces>
<!-- Source components under test. All live in web/src/features/activity/ -->

From web/src/features/activity/ReactionSummaryPills.tsx:
```tsx
import { EMOJI_MAP, type EmojiType } from '../reactions/ReactionPicker';
interface ReactionSummaryPillsProps {
  reactionSummary?: Record<string, number>;
}
export function ReactionSummaryPills({ reactionSummary }: ReactionSummaryPillsProps)
// Renders "No reactions" when empty/undefined, otherwise emoji+count pills
```

From web/src/features/activity/RecordingSlider.tsx:
```tsx
export interface ActivitySession {
  sessionId: string;
  userId: string;
  sessionType: 'BROADCAST' | 'HANGOUT';
  thumbnailUrl?: string;
  recordingDuration?: number;
  createdAt: string;
  endedAt?: string;
  reactionSummary?: Record<string, number>;
  participantCount?: number;
  messageCount?: number;
  recordingStatus?: 'pending' | 'processing' | 'available' | 'failed';
  aiSummary?: string;
  aiSummaryStatus?: 'pending' | 'available' | 'failed';
}
export function RecordingSlider({ sessions }: { sessions: ActivitySession[] })
// Filters to sessionType === 'BROADCAST' only; "No recordings yet" empty state
// Renders scroll-snap container; each card navigates to /replay/:sessionId
```

From web/src/features/activity/ActivityFeed.tsx:
```tsx
export function ActivityFeed({ sessions }: { sessions: ActivitySession[] })
// Sorts by endedAt DESC; dispatches BroadcastActivityCard or HangoutActivityCard
// "No activity yet" empty state
```

From web/src/features/activity/BroadcastActivityCard.tsx:
```tsx
export function BroadcastActivityCard({ session }: { session: ActivitySession })
// Renders userId, formatDuration (MM:SS), formatDate (relative), ReactionSummaryPills, SummaryDisplay
// onClick navigates to /replay/:sessionId
```

From web/src/features/activity/HangoutActivityCard.tsx:
```tsx
export function HangoutActivityCard({ session }: { session: ActivitySession })
// Renders userId, participantCount (with plural), messageCount (with plural), formatDuration, formatDate
// onClick navigates to /replay/:sessionId
```

Existing test pattern (from web/src/features/activity/BroadcastActivityCard.test.tsx at project root of activity/):
- Uses vitest: `import { describe, it, expect, vi, beforeEach } from 'vitest'`
- Uses @testing-library/react: `import { render, screen } from '@testing-library/react'`
- Wraps in BrowserRouter for useNavigate
- Mocks react-router-dom useNavigate with vi.mock
- Uses ActivitySession type from RecordingSlider for mock data

Existing test pattern (from web/src/features/replay/__tests__/ReplayViewer.test.tsx):
- Mocks aws-amplify/auth, react-router-dom
- Uses global.fetch mock
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReactionSummaryPills, RecordingSlider, and ActivityFeed test files</name>
  <files>
    web/src/features/activity/__tests__/ReactionSummaryPills.test.tsx
    web/src/features/activity/__tests__/RecordingSlider.test.tsx
    web/src/features/activity/__tests__/ActivityFeed.test.tsx
  </files>
  <action>
Create 3 vitest test files following the established pattern (vitest imports, @testing-library/react, BrowserRouter wrapper, vi.mock for react-router-dom useNavigate).

**ReactionSummaryPills.test.tsx** (mock EMOJI_MAP via vi.mock on '../reactions/ReactionPicker'):
- Renders "No reactions" when reactionSummary is undefined
- Renders "No reactions" when reactionSummary is empty object {}
- Renders correct number of pills for { heart: 42, fire: 17 }
- Displays count numbers in pills (screen.getByText('42'))

**RecordingSlider.test.tsx** (mock useNavigate, mock ReactionSummaryPills as passthrough):
- Renders "No recordings yet" when sessions is empty array
- Renders "No recordings yet" when all sessions are HANGOUT type (filter removes them)
- Renders only BROADCAST sessions (pass 2 BROADCAST + 1 HANGOUT, verify HANGOUT userId not in document)
- Renders "Recent Broadcasts" heading
- Renders userId and formatted duration for each broadcast card
- Renders scroll-snap container class (overflow-x-auto, snap-x)

**ActivityFeed.test.tsx** (mock BroadcastActivityCard and HangoutActivityCard):
- Renders "No activity yet" when sessions is empty array
- Sorts sessions by endedAt DESC (pass sessions in wrong order, verify DOM order via getAllByTestId or textContent sequence)
- Renders BroadcastActivityCard for BROADCAST sessions and HangoutActivityCard for HANGOUT sessions (verify by mock call args or rendered content)

All files use the same mock pattern as the existing BroadcastActivityCard.test.tsx at the activity/ root level. Import ActivitySession from '../RecordingSlider' (relative path from __tests__/).
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run src/features/activity/__tests__/ReactionSummaryPills.test.tsx src/features/activity/__tests__/RecordingSlider.test.tsx src/features/activity/__tests__/ActivityFeed.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>3 test files exist in web/src/features/activity/__tests__/, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create BroadcastActivityCard and HangoutActivityCard test files</name>
  <files>
    web/src/features/activity/__tests__/BroadcastActivityCard.test.tsx
    web/src/features/activity/__tests__/HangoutActivityCard.test.tsx
  </files>
  <action>
Create 2 vitest test files following the established pattern.

**BroadcastActivityCard.test.tsx** (mock useNavigate, mock ReactionSummaryPills and SummaryDisplay as passthroughs):
- Renders session userId as heading text
- Renders formatted duration "2:00" for recordingDuration 120000
- Renders relative timestamp (use Date.now() - 3600000 for "1h")
- Renders "unknown" when recordingDuration is undefined
- Calls navigate('/replay/test-session-123') on click (use fireEvent.click, verify mockNavigate was called with correct path)
- Renders ReactionSummaryPills with session.reactionSummary prop

**HangoutActivityCard.test.tsx** (mock useNavigate, mock SummaryDisplay as passthrough):
- Renders session userId as heading text
- Renders participantCount with plural handling: "3 participants" vs "1 participant"
- Renders messageCount with plural handling: "5 messages" vs "1 message"
- Renders formatted duration and relative timestamp
- Defaults participantCount and messageCount to 0 when undefined
- Calls navigate('/replay/test-session-123') on click

Note: The existing BroadcastActivityCard.test.tsx at web/src/features/activity/BroadcastActivityCard.test.tsx (root level, not __tests__/) covers AI summary display. The __tests__/ version should cover the core card rendering behavior (userId, duration, reactions, timestamp, navigation). Do NOT duplicate the AI summary tests -- those are already covered by the root-level file.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run src/features/activity/__tests__/BroadcastActivityCard.test.tsx src/features/activity/__tests__/HangoutActivityCard.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>2 test files exist in web/src/features/activity/__tests__/, all tests pass</done>
</task>

</tasks>

<verification>
Run all 5 test files together to confirm no conflicts:
```bash
cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run src/features/activity/__tests__/ --reporter=verbose
```

Verify all 5 files exist:
```bash
ls -la /Users/connorhoehn/Projects/videonowandlater/web/src/features/activity/__tests__/
```

Run the full web test suite to confirm no regressions:
```bash
cd /Users/connorhoehn/Projects/videonowandlater/web && npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- All 5 test files exist in web/src/features/activity/__tests__/
- All tests pass via vitest
- No regressions in existing test suite (ReplayViewer tests, root-level BroadcastActivityCard.test.tsx)
- The gap identified in 18-VERIFICATION.md is fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/18-homepage-redesign-activity-feed/18-04-SUMMARY.md`
</output>
