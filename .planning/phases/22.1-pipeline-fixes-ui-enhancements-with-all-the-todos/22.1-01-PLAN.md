---
phase: 22.1-pipeline-fixes-ui-enhancements-with-all-the-todos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [infra/lib/constructs/ivs-cleanup-resource.ts, infra/lib/stacks/live-stack.ts]
autonomous: true
requirements: []
must_haves:
  truths:
    - "CDK stack deletion completes without manual intervention"
    - "IVS channels detach from recording configurations automatically"
    - "No orphaned IVS resources remain after stack deletion"
  artifacts:
    - path: "infra/lib/constructs/ivs-cleanup-resource.ts"
      provides: "Custom resource Lambda for IVS cleanup"
      min_lines: 50
    - path: "infra/lib/stacks/live-stack.ts"
      provides: "Custom resource integration in CDK stack"
      contains: "IvsCleanupResource"
  key_links:
    - from: "infra/lib/stacks/live-stack.ts"
      to: "infra/lib/constructs/ivs-cleanup-resource.ts"
      via: "import and instantiation"
      pattern: "new IvsCleanupResource"
---

<objective>
Add CDK custom resource to automatically clean up IVS resources before stack deletion.

Purpose: Prevent stack deletion failures due to IVS resource dependencies
Output: Custom resource construct that detaches recording configurations from channels
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-pipeline-fixes-ui-enhancements-with-all-the-todos/22.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IVS cleanup custom resource construct</name>
  <files>infra/lib/constructs/ivs-cleanup-resource.ts, infra/lib/constructs/ivs-cleanup-resource.test.ts</files>
  <behavior>
    - Test 1: Custom resource triggers on CloudFormation DELETE event only
    - Test 2: Lists all IVS channels and detaches recording configurations
    - Test 3: Returns success response even if no channels exist
    - Test 4: Handles IVS API errors gracefully with logging
  </behavior>
  <action>
    Create a CDK custom resource construct that implements IVS cleanup on stack deletion. The Lambda handler should:
    1. Only execute on CloudFormation DELETE events (ignore CREATE/UPDATE)
    2. Use AWS SDK v3 IVS client to list all channels
    3. For each channel with a recording configuration, call updateChannel to set recordingConfigurationArn to empty string
    4. Return PhysicalResourceId: 'ivs-cleanup-complete' on success
    5. Log all operations and handle errors gracefully

    Use AwsCustomResource from aws-cdk-lib/custom-resources with inline Lambda code.
    Grant the Lambda role permissions: ivs:ListChannels, ivs:GetChannel, ivs:UpdateChannel.
  </action>
  <verify>
    <automated>cd infra && npm test -- ivs-cleanup-resource.test.ts</automated>
  </verify>
  <done>Custom resource construct created with tests passing, handles DELETE events and detaches recording configs</done>
</task>

<task type="auto">
  <name>Task 2: Integrate cleanup resource into CDK stack</name>
  <files>infra/lib/stacks/live-stack.ts</files>
  <action>
    Integrate the IVS cleanup custom resource into the LiveStack:
    1. Import the IvsCleanupResource construct from '../constructs/ivs-cleanup-resource'
    2. Instantiate the custom resource early in the stack (before recording configuration)
    3. Add an explicit dependency: make recording configuration depend on the cleanup resource
    4. This ensures cleanup resource is deleted AFTER recording config during stack deletion

    Position the resource right after the IVS resource pool creation.
  </action>
  <verify>
    <automated>cd infra && npm run build && cdk synth LiveStack | grep -A5 "IvsCleanup"</automated>
  </verify>
  <done>Cleanup resource integrated into LiveStack, synthesis shows custom resource in CloudFormation template</done>
</task>

</tasks>

<verification>
- CDK synthesis succeeds with custom resource in template
- Test deployment: `cdk deploy LiveStack --hotswap` completes
- Test deletion: Create test channel with recording config, then verify cleanup on stack destroy
</verification>

<success_criteria>
- IVS cleanup custom resource construct exists with tests
- Custom resource integrated into LiveStack
- Stack deletion no longer requires manual IVS cleanup
- All existing functionality remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-pipeline-fixes-ui-enhancements-with-all-the-todos/22.1-01-SUMMARY.md`
</output>