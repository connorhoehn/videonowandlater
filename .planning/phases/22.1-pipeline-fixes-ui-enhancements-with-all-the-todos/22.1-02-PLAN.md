---
phase: 22.1-pipeline-fixes-ui-enhancements-with-all-the-todos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/handlers/store-summary.ts, backend/src/handlers/store-summary.test.ts]
autonomous: true
requirements: []
must_haves:
  truths:
    - "AI summaries use Nova Pro model instead of Claude"
    - "Summary generation continues to work with same quality"
    - "Model configuration is environment-based for flexibility"
  artifacts:
    - path: "backend/src/handlers/store-summary.ts"
      provides: "Nova Pro integration for AI summaries"
      contains: "amazon.nova-pro"
    - path: "backend/src/handlers/store-summary.test.ts"
      provides: "Tests for Nova Pro integration"
      min_lines: 10
  key_links:
    - from: "backend/src/handlers/store-summary.ts"
      to: "BEDROCK_MODEL_ID environment variable"
      via: "process.env lookup"
      pattern: "process\\.env\\.BEDROCK_MODEL_ID"
---

<objective>
Switch AI summary generation from Claude to Amazon Nova Pro for better cost/performance.

Purpose: Optimize AI processing costs while maintaining summary quality
Output: Updated store-summary handler using Nova Pro model
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-pipeline-fixes-ui-enhancements-with-all-the-todos/22.1-RESEARCH.md

<interfaces>
<!-- Key types and existing patterns from store-summary.ts -->
From backend/src/handlers/store-summary.ts (current implementation):
- Uses BedrockRuntimeClient with InvokeModelCommand
- Currently uses 'anthropic.claude-3-haiku-20240307-v1:0' model
- Payload format: { anthropic_version, max_tokens, messages, temperature }
- Stores summary in DynamoDB session.aiSummary field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update store-summary handler to use Nova Pro</name>
  <files>backend/src/handlers/store-summary.ts, backend/src/handlers/store-summary.test.ts</files>
  <behavior>
    - Test 1: Handler uses Nova Pro model ID from environment or default
    - Test 2: Nova Pro payload format is correctly structured
    - Test 3: Summary generation succeeds with Nova Pro response format
    - Test 4: Fallback to default model if env var not set
  </behavior>
  <action>
    Update the store-summary handler to use Amazon Nova Pro:
    1. Change default model ID from Claude to 'amazon.nova-pro-v1:0'
    2. Add environment variable support: process.env.BEDROCK_MODEL_ID || 'amazon.nova-pro-v1:0'
    3. Update payload format for Nova Pro (check AWS docs for exact format):
       - Nova uses different structure than Anthropic models
       - Likely: { prompt, max_tokens: 500, temperature: 0.7 }
    4. Parse Nova Pro response format (different from Claude response)
    5. Keep same summary storage logic in DynamoDB

    Ensure backward compatibility: if BEDROCK_MODEL_ID is set to Claude, use Claude format.
    Add comprehensive logging for model selection and payload structure.
  </action>
  <verify>
    <automated>cd backend && npm test -- store-summary.test.ts</automated>
  </verify>
  <done>Handler updated to use Nova Pro, tests pass with new model format</done>
</task>

<task type="auto">
  <name>Task 2: Update CDK stack environment for Nova Pro</name>
  <files>infra/lib/stacks/session-stack.ts</files>
  <action>
    Update the Lambda environment variables in SessionStack:
    1. Find the storeSummaryFunction definition
    2. Add BEDROCK_MODEL_ID environment variable: 'amazon.nova-pro-v1:0'
    3. Ensure the Lambda has bedrock:InvokeModel permission for Nova Pro model
    4. The permission should already be broad enough ('bedrock:InvokeModel' on '*')

    This ensures production deployments use Nova Pro by default.
  </action>
  <verify>
    <automated>grep -n "BEDROCK_MODEL_ID" infra/lib/stacks/session-stack.ts</automated>
  </verify>
  <done>CDK stack updated with Nova Pro model configuration</done>
</task>

</tasks>

<verification>
- Tests pass with Nova Pro model format
- CDK synthesis includes BEDROCK_MODEL_ID environment variable
- Manual test: Trigger summary generation, verify Nova Pro in CloudWatch logs
</verification>

<success_criteria>
- AI summaries generated using Nova Pro model
- Environment-based model configuration working
- Summary quality maintained (one-paragraph summaries)
- Tests updated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-pipeline-fixes-ui-enhancements-with-all-the-todos/22.1-02-SUMMARY.md`
</output>