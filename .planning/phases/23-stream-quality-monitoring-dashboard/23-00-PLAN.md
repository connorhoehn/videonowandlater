---
phase: 23-stream-quality-monitoring-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/domain/__tests__/metrics.test.ts
  - web/src/features/broadcast/__tests__/useStreamMetrics.test.tsx
  - web/src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx
  - web/src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx
  - web/src/features/replay/__tests__/ReplayPage.integration.test.tsx
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - Test scaffolds exist for all domain models and components before implementation
    - Test files use vitest and @testing-library/react conventions
    - All test files compile without TypeScript errors
  artifacts:
    - path: "web/src/domain/__tests__/metrics.test.ts"
      provides: "Test scaffold for metrics domain model and calculateHealthScore"
      min_lines: 20
    - path: "web/src/features/broadcast/__tests__/useStreamMetrics.test.tsx"
      provides: "Test scaffold for useStreamMetrics hook"
      min_lines: 20
    - path: "web/src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx"
      provides: "Test scaffold for StreamQualityDashboard component"
      min_lines: 20
    - path: "web/src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx"
      provides: "Test scaffold for StreamQualityOverlay component"
      min_lines: 20
    - path: "web/src/features/replay/__tests__/ReplayPage.integration.test.tsx"
      provides: "Test scaffold for backward compatibility integration test"
      min_lines: 20
  key_links:
    - from: "web/src/domain/__tests__/metrics.test.ts"
      to: "vitest"
      via: "Test runner import"
      pattern: "import.*describe.*from 'vitest'"
---

<objective>
Create test scaffolds for all Phase 23 components and hooks. This Wave 0 plan ensures all test files exist before the implementation tasks reference them in their verify commands. The scaffolds provide the minimal structure needed for TDD workflow.

Purpose: Enable TDD approach by creating test files first, preventing "file not found" errors during test execution.
Output: Five test scaffold files ready for implementation
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.4-ROADMAP.md

<!-- Existing test patterns to follow -->
@web/src/features/broadcast/__tests__/BroadcastPage.test.tsx
@web/src/features/chat/__tests__/ChatBox.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for all Phase 23 components</name>
  <files>web/src/domain/__tests__/metrics.test.ts, web/src/features/broadcast/__tests__/useStreamMetrics.test.tsx, web/src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx, web/src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx, web/src/features/replay/__tests__/ReplayPage.integration.test.tsx</files>
  <action>
Create test scaffold files that will be filled in by subsequent plans:

**1. Create `web/src/domain/__tests__/metrics.test.ts`:**
```typescript
import { describe, it, expect } from 'vitest';
import { calculateHealthScore } from '../metrics';

describe('metrics domain', () => {
  describe('calculateHealthScore', () => {
    it.todo('returns ~100 score with perfect metrics');
    it.todo('detects bitrate-drop warning when bitrate falls >30%');
    it.todo('detects fps-drop warning when FPS is low');
    it.todo('penalizes high jitter in samples');
  });

  describe('stdDev helper', () => {
    it.todo('calculates standard deviation correctly');
  });
});
```

**2. Create `web/src/features/broadcast/__tests__/useStreamMetrics.test.tsx`:**
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook } from '@testing-library/react';
import { useStreamMetrics } from '../useStreamMetrics';

describe('useStreamMetrics', () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it.todo('returns null metrics when not live');
  it.todo('polls stats every 5 seconds when isLive=true');
  it.todo('extracts outbound-rtp video stats from WebRTC');
  it.todo('maintains 60-sample rolling window');
  it.todo('calculates health score after 3+ samples');
  it.todo('cleans up interval on unmount');
});
```

**3. Create `web/src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx`:**
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { StreamQualityDashboard } from '../StreamQualityDashboard';

describe('StreamQualityDashboard', () => {
  const mockMetrics = {
    timestamp: Date.now(),
    bitrate: 2500000,
    framesPerSecond: 30,
    resolution: { width: 1920, height: 1080 },
    networkType: 'wifi',
    qualityLimitation: 'none',
  };

  const mockHealthScore = {
    score: 95,
    bitrateHealth: 96,
    fpsHealth: 94,
    warning: 'none' as const,
  };

  it.todo('renders health score circle with percentage');
  it.todo('applies green color for score ≥80');
  it.todo('applies yellow color for score 60-79');
  it.todo('applies red color for score <60');
  it.todo('shows warning badge when warning !== none');
  it.todo('expands to show detailed metrics');
  it.todo('returns null when not live');
});
```

**4. Create `web/src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx`:**
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { StreamQualityOverlay } from '../StreamQualityOverlay';

describe('StreamQualityOverlay', () => {
  const mockMetrics = {
    timestamp: Date.now(),
    bitrate: 2500000,
    framesPerSecond: 30,
    resolution: { width: 1920, height: 1080 },
    networkType: 'wifi',
    qualityLimitation: 'none',
  };

  const mockHealthScore = {
    score: 95,
    bitrateHealth: 96,
    fpsHealth: 94,
    warning: 'none' as const,
  };

  it.todo('positions overlay at bottom-right with z-40');
  it.todo('returns null when isLive=false');
  it.todo('returns null when metrics=null');
  it.todo('returns null when healthScore=null');
  it.todo('renders StreamQualityDashboard inside overlay');
});
```

**5. Create `web/src/features/replay/__tests__/ReplayPage.integration.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
// Note: ReplayPage import will be added in Task 3 of plan 03

describe('ReplayPage backward compatibility', () => {
  it.todo('loads Phase 1-22 recording without streamMetrics field');
  it.todo('handles missing streamMetrics gracefully without errors');
});
```

Verify all files compile:
```bash
cd web && npm run type-check
```
  </action>
  <verify>
    <automated>cd web && ls -la src/domain/__tests__/metrics.test.ts src/features/broadcast/__tests__/useStreamMetrics.test.tsx src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx src/features/replay/__tests__/ReplayPage.integration.test.tsx</automated>
  </verify>
  <done>Test scaffold files created for all Phase 23 components; ready for TDD implementation</done>
</task>

</tasks>

<verification>
Verify all test files exist and TypeScript compiles:
```bash
cd web && npm run type-check
```

List created test files:
```bash
find web/src -name "*.test.ts*" -path "*__tests__*" | grep -E "(metrics|StreamQuality|ReplayPage\.integration)" | sort
```
</verification>

<success_criteria>
- [ ] web/src/domain/__tests__/metrics.test.ts exists with test scaffolds
- [ ] web/src/features/broadcast/__tests__/useStreamMetrics.test.tsx exists with hook test scaffolds
- [ ] web/src/features/broadcast/__tests__/StreamQualityDashboard.test.tsx exists with component test scaffolds
- [ ] web/src/features/broadcast/__tests__/StreamQualityOverlay.test.tsx exists with overlay test scaffolds
- [ ] web/src/features/replay/__tests__/ReplayPage.integration.test.tsx exists with integration test scaffolds
- [ ] All test files use vitest and @testing-library/react conventions
- [ ] TypeScript compilation succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-stream-quality-monitoring-dashboard/23-00-SUMMARY.md`
</output>